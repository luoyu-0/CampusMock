# 成员 A：前端开发记录

> 集成更新（2026-09-10）：成员 D 已将临时 `src/state/store.ts` 替换为 `src/storage/`，并接入可靠保存、过期响应拦截、多标签页冲突和坏档恢复。下文涉及“等待成员 D”的内容保留为成员 A 当时的交接记录，当前状态以 [成员 D 集成记录](成员-D-集成.md) 为准。

## 当前任务

- 界面原型已出第一版，等团队过一遍视觉与状态划分。
- 主视觉方向已从「手绘内联 SVG」换成「AI 生成插画 + CSS 动效」，开始页与结局页两处都已替换，等确认是否作为最终视觉基调。
- 桌面端两侧远景已从内联 SVG 换成 5 套 AI 生成水彩小景（切屏随机换，兜底仍留着一层 SVG），等团队看过一遍：水彩是否比矢量更贴合「心理治愈」基调、透明度 .45 是否过淡、以及 12 张图合计 642 KB 的体积能否接受。
- **上一轮那批「未提交」的东西现在已经在主干上了**：`client/` 的页面、状态机、走查条与文档经 PR #4 合入 `main`，成员 D 又把临时的 `src/state/store.ts` 换成了 `src/storage/`。下面「已完成内容」里凡涉及 `store.ts` 与「未调用后端」的表述是当时的记录，以本段为准。
- **本轮（2026-09-11）做的两件事**：① 用真实 Express 把 14 天 + 异常与重复操作端到端跑了一遍（数据见验证记录）；② 给走查面板补 `⑥ 顶格长文案`、`⑦ 顶格结局` 两个态，按成员 C `server/src/ai/prompts/values.ts` 里的字段上限把文字填满。
- **上一轮那条「`controller.ts` 一行都没 import `src/ai/`」已经过期**：成员 B 的 PR #13（合并于 `7ee4b00`）把生成事件与生成结局都接到了 C 的模块上，`server/src/game/controller.ts:27-32` 现在确实从 `../ai/index` 导入 `generateEvent` / `generateEnding` / `loadAiConfig`。所以顶格夹具不再是「等接线那天才用得上」，而是现在就能拿来验真实模型排版的现成数据。界面侧唯一的未知仍然是真实输出的排版。
- **接线换出来一个新的前置条件**：`controller.ts` 在没配 `DEEPSEEK_API_KEY` 时返回 `AI_CONFIG_ERROR` 且 `retryable: false`，界面停在**不可重试**屏。今晚实测确认呈现是「这本日记我读不懂 / AI config not loaded, check DEEPSEEK_API_KEY / 查看处理说明」，不是可重试那一屏（上一版这里写反了，已改）。顺带一条契约缺陷：`sendError` 的 message 是英文，而 `docs/接口约定.md:50` 要求提示用简体中文——#19 只把 AI 那条分支换成透传 `err.message`，其余仍是英文。
- **本轮（2026-09-11 深夜）第三件事：正文排版整体改衬线**，走「纸质刊物」方向，单文件 `client/src/styles/global.css` +13/−8，分支 `feat/client-prose-serif` 已推、PR 待评审。逐条取值与两条踩过的坑见「已完成内容」最后一条。
- **第四件事（2026-09-12 凌晨）：玩家档案对接**。成员 C 早在 `server/src/ai/schema.ts:54` 留了 `PlayerProfile { gender, major }`，提示词两侧也都有「传了就用、没传就走中性表述」的分支（`prompts/event.ts:48-50`、`prompts/ending.ts:52-54`），C 的 README 里那句「收集时机待与成员 C 约定」一直没人认领——**从路由层到界面这一段是空的**。前端这侧补齐：开始页开局问一次、单独一个 localStorage 键、随两个生成请求的顶层 `profile` 发出，共 6 个文件 +158/−15。**按负责人定的节奏没另开分支，与上面的字体改动合在 `feat/client-prose-serif` 同一个 PR**，代价是这个 PR 跨了两件事，评审可以按两个提交分开看。要说清楚：**这条链路目前端到端仍然无效**——`controller.ts:87` 只解构 `requestId, snapshot`，我发过去的 `profile` 会被直接丢弃（不报错、也不生效）。缺的是 B 那边两行，见待协作 23。**（这一句到 #24 已经过期：B 接上了，档案现在真会进提示词，详见待协作 23 的闭环记录。）**
- **第五件事（2026-09-12 凌晨）：把不可重试错误屏拆成两种**。对比最新 `main` 时发现上面那条「没配密钥会停在不可重试屏」的后果我之前只记了现象、没管呈现：那一屏的按钮与文案**整套都是围绕「本机存档坏了」写的**（查看处理说明 → 建议导出原始存档并检查浏览器是否允许本站使用本机存储），而 `AI_CONFIG` 这类服务端问题落到它上面，等于让玩家去查一个没坏的浏览器。更糟的是这一屏**没有任何出口**——顶栏只在 `retryable` 时渲染，按钮里没有导航，玩家只能刷新。现在按 `error.code` 是否在「存档类白名单」里分成两屏：存档类保持原样，其余不可重试错误给主按钮「先回到上一页」＋一句不误导的说明。顺手修掉一个我会继承到的老坑：`dismissError()` 在 `pendingEvent` / `pendingEnding` 两态只清错误，会把玩家送回一张**没有任何按钮的转圈骨架屏**（`EventWaitingCard` 里确实一个控件都没有），这两态下退出改为回开始页，从「继续上次的日记」重新发起。这条也并入 `feat/client-prose-serif` 同一个 PR。
- **第六件事（2026-09-12 中午）：结算页点「继续写下一篇」之后没有任何反馈**。用户实测提出来的。查下来不是错觉而是缺样式：`global.css` 的 `.btn` 只写了 `:hover` / `.ghost` / `.muted`，**没有 `:disabled` 也没有 `:active`**，所以请求在飞的那十几秒里那颗蓝色渐变按钮长得跟能点的时候一模一样，只是点不动。现在按他选定的形态改：等待时按钮与那行提示一起撤掉，换成一句「正在写第 N 天」＋一条 4px 暖橙流光条（颜色借顶栏精力那颗的 `--energy`，不新增变量），同时给刚读完的结算卡套一层 `.turning`（`opacity:.55`）做出翻页感。**流光条刻意做成不按比例的循环**——我们并不知道模型写到哪了，假装进度比不装更糟。三条边界按他说的守着：① 不要二级提示，一句到底；② 只改这一颗按钮，`EventCard` 那三颗选项保持原样；③ **状态机一行没动**，`snapshot.phase` 仍然是 `showResult`，只是渲染层按 `busy` 换形态，这期间刷新照样回到结算页、天数不推进。传进来的 prop 用的是 `useGame` 原始的 `busy` 而不是 `interactionBlocked`——后者在写档失败时也为 `true`，会把「正在写」和「正在保存」混成一句话。共 3 个文件，`GamePage.tsx` 一个分支、`App.tsx` 一行、`global.css` 一组样式。
- **第七件事（2026-09-12 下午）：第 14 天的结局死锁修掉了（待协作 25）**。先拉最新 `main`（#23 C 的流式底座 + #24 B 的 PlayerProfile，合并提交 `1a61a44`，进来的 12 个文件 / +847 / −70 全在 `server/` 与文档，前端一行没被改），再按负责人那句「帮我解决」动手。结论比原来记的乐观：**故障只在渲染层**，请求层一直是好的，所以改动只有 2 个文件——`GamePage.tsx` 一个派生布尔（结算屏的门放宽到也吃 `pendingEnding && !waiting`）+ `useGame.ts` 里 `dismissError()` 的特例收窄，`dispatch` / 五要素比对 / 幂等 / 存档一行未动，B 侧零依赖。两种走法各自的代价都躲开了：第 14 篇结算文案保住、`controller.ts` 不必回退成 `showResult`，顺带把"刷新也救不回来"那半段一起治好。**按 BC 已入库的接口逐处对过，前端需要改的是零**：`profile` 的请求体形状与双侧归一语义一致（详见待协作 23）、`chosenText` 服务端截首行与我界面 `splitOption` 的取法相同、C 的流式还没进 HTTP 所以 NDJSON 那条约定继续有效（待协作 26）。**#24 带回两条回归，都在别人文件里、我只报不动**：`loadAiConfig()` 回到模块加载期会当场打死演示（新提的待协作 27，优先级最高），以及 `handleAiError` 不再透传 `err.message`、C 刚写好的四句玩家文案一句都到不了界面（待协作 24 已更新）。
- **第八件事（2026-09-12 傍晚）：第二次拉主干，确认第 14 天与流式各自的真实状态**。拉下来的是 `69c6d44`（#26），三件事同时清楚了：① **我那个修复没进 #25**——push 在本机连不上 GitHub 的那几分钟里负责人把 #25 合了，所以**主干上第 14 天仍然死锁**，`origin/main` 的 `GamePage.tsx` 里 `settledScreen` 根本不存在，`controller.ts:206-207` 第 14 次 `choose` 照样返回 `pendingEnding`。`f89a90b` 只在本地，**需要另开一张 PR**（#25 已合并，那张分支上不再挂着开放 PR）。② #26 一次闭环了我报的**待协作 24 和 27**（错误文案透传 + `.env` 装载，新增的是 `server/src/env.ts` 按 `__dirname` 定位，比我给的方案稳），我记的"必须从仓库根 cwd 起服务"那条绕法当场作废。③ **流式效果仍未实现**，而且这次的判定不是"读代码觉得没接"，是四处独立 grep 一起指向同一结论，详见待协作 26。把 `origin/main` 并进本分支（`d2b542f`，无冲突），并完复验：#26 动的文件全在 `server/` 与文档，**前端零影响**（构建产物 hash 与并分支前逐字相同），第 14 天那套渲染断言在合并后的代码上重新跑一遍仍全通。
- **第九件事（2026-09-12 晚）：流式的前端消费段做完了（待协作 26 的前端那一段闭环，服务端那一段仍空）**。负责人选了"真流式"，所以我按"前端两种响应都吃"落地：服务端今天返回整块 JSON 就走原路、行为一个字节不变（断言见验证记录，玩家可见的 HTML 与主干上那张老卡逐字相同），哪天 B 把 `generateEventStream` 接进 `controller.ts` 并改成 NDJSON，字就开始动，前端不用再改。**帧约定比我傍晚那版改了一次，理由要说清**：原稿写的是"转发上游原始片段 `{"k":"delta","t":…}`、C 不解析 JSON"——那是在我读全 `streamParse.ts` 之前定的，C 其实已经把残缺 JSON 解析成语义回调了（`onTitle` 整体一次 / `onDescriptionDelta` 增量 / `onOptionText` 整条弹出 / `onResultTextDelta` 与 `onResultText` / `onRetry(attempt)`，共 6 个，见 `server/src/ai/generateEvent.ts:35-42`），再转发生 token 等于把 C 做完的活退回给浏览器去解析一遍。所以现在的帧就是他那几个回调的形状：`title` / `desc` / `opt` / `reset` / 终帧 `end`（**终帧键名从 `done` 改成了 `end`，那版本没人实现过，改名无成本**），终帧内容仍是今天那个完整响应体一个字段不改，五要素比对、只在完整响应后写档、重试幂等全部原样保留；未知 `k` 一律忽略，所以 `resultText` 那两个回调 B 发与不发都不会弄坏界面（下面 26 里补了这两个的取舍，我原稿漏了）。**只有事件生成这一路带帧回调**，因为 C 有 `generateEventStream` 没有 `generateEndingStream`（`generateEnding.ts` 搜 `Stream` 零命中）——结局那一路即使哪天也流起来，读帧逻辑照样能把终帧取回来，只是不画草稿。**草稿不进存档**：新增的是 `useGame` 里一个临时 `draft`，终帧一到就被真快照取代，`reset` 清空，刷新即无；`dispatch` 给回调加了两道闸（`settled` + `requestGeneration`），否则上一天写了一半的字会串进下一天的等待屏。**假服务也逐帧输出**，而且刻意摊在它本来就要等的那 420~1150ms 里，所以开流式没有让任何一天变慢（实测 6 帧 / 997ms），今晚没网没密钥也能在 dev 里看到这个效果。共 6 个文件，新增加 `client/src/api/frames.ts`。
- **同轮顺手修掉我自己文件里一处过期断言**：`mockApi.ts` 的 `fatalServer` 分支注释写着"code 与文案都照抄真服务"，实际还停在 `AI_CONFIG` +「缺少环境变量 DEEPSEEK_API_KEY…」——那是 #26 之前的老服务端文案，现在 `controller.ts:79` 把 code 改名成 `AI_CONFIG_ERROR`、`deepseek.ts:46` 抛的是「心神不定，不知如何落笔（未配置API密钥）」。走查面板那颗「下次请求·服务端不可重试」从此真的和线上同一屏。另一颗 `retryable` 用的 `MODEL_TIMEOUT` **服务端根本不存在这个 code**（全仓 `server/src` 零命中），它是原型自造的可重试样例，我没改成真的，因为这一屏要验的是"可重试那一屏长什么样"而不是某个具体 code；留着这条差异，记在这里。
- 我这轮能独立做完的做完了。事实更新：`DEEPSEEK_API_KEY` 已配进本机 `.env`（不入库），PR #19 把 `loadAiConfig()` 挪到请求阶段（此前 `npm run dev:server` 因 cwd 在 `server/` 根本读不到根目录 `.env`；#24 曾把它挪回模块加载期导致同一条故障复活——**傍晚 #26 已彻底修好**：新增 `server/src/env.ts` 按 `__dirname` 解析根目录 `.env`，见待协作 27 的闭环记录，**"必须从仓库根 cwd 起服务"那条绕法作废**），PR #17 让 `choose` 保留 `currentEvent`（`validateSnapshot` 的 `showResult` 分支要求它非空，前端不用改）。**剩下的**：`⑥⑦` 两个顶格态在屏幕上的实际折行仍需人眼过一遍；真实模型输出下的衬线观感没验过；真机局域网验收（D）。`README.md` 与 `Deploy.md` 由负责人维护，我没动。
- 阻塞条件：`shared/` 公共类型归属仍未定（见待协作事项 1）。实测对过一遍表：我这边的 `client/src/state/types.ts` 与 B 的 `server/src/game/types.ts` **字段名与结构完全一致**，只有类型名不同（`GameEvent`↔`EventData`、`GameOption`↔`EventOption`、`HistoryEntry`↔`HistoryRecord`、`Ending`↔`EndingResult`），前端不需要改字段，定归属时按 B 的命名走即可。

## 已完成内容

- `client/design/prototype.html`：单文件原型，零构建、不引框架，双击即开。覆盖 5 个 `phase` + 两类错误 + 开始页共 8 屏，内置走查状态切换条与「设计标注」开关。
- `client/design/assets/hero-campus.webp`（43 KB）、`assets/ending-dusk.webp`（58 KB）：开始页校门与结局页林荫道两张 AI 生成插画，同一天空色温、同一套秋季暖调，保证两屏视觉连续。
- `client/design/assets/side-{1..5}-{l,r}.webp`：桌面端两侧远景 5 套主题 × 左右共 10 张 AI 水彩小景，542 KB（单张 35~77 KB）。生成时统一限定「柔和笔触、低饱和、象牙白底、画面内不得出现任何文字」，所以十张是一个调子；不出文字这条同时保护了「学业/社交 在选定前不得泄露」的规则，也避开了 AI 写中文必糊的问题。
- 插画采用「位图在上、手绘内联 SVG 在下」四层结构：`<img>` 加载失败自动 `remove()` 露出下层矢量图，所以只把 HTML 单文件发给别人也不会白屏；代价是分享 `design` 目录时要整个文件夹一起给。
- 动效全部是纯 CSS 关键帧，共 6 条：插画 42 秒极缓推近、落日暖光 9 秒呼吸、落叶飘落（开始页 5 片 / 结局页 4 片，位置与节奏由 `<i>` 上的 CSS 变量控制）、两侧远景 23~28 秒漂移、日记纸页 15~22 秒漂移、换背景时 1 秒淡入。JS 只写 CSS 自定义属性、`data-scene` 和按需补上的图片 `src`，没有逐帧动画。`prefers-reduced-motion: reduce` 时全部关闭。
- 修掉一个既有布局缺陷：结局页横幅的 `margin-bottom:-22px` 会把「结局 · 9 月 14 日」拉进图片里重叠约 10px，现改为横幅与正文间留 22px。
- 桌面端开始页两侧留白已填，并且**做了 5 套主题、切屏随机换**，避免每一屏两侧长一个样：① 宿舍台阶与上楼的两人 / 亮灯的教学楼与门口的自行车 ② 梧桐林荫道与空长椅 / 银杏树下挂着书包的自行车 ③ 拱形门廊与台阶上睡着的猫 / 摞起的书、摊开的日记和一杯热饮 ④ 看台与躺在跑道栏杆边歇着的人 / 足球门与靠门柱的人 ⑤ 湖心亭垂柳与小桥上坐着的猫 / 石拱桥、桥头长椅与芦苇。取的是「一个人路过时瞥见的校园细节」这一类画面，透明度 .45。
- 位图是透明水彩小景、盖不住下面的矢量，所以位图 `onload` 后给容器加 `has-art`，用 `.scene .side.has-art svg{display:none}` 把兜底图收起来；`onerror` 只 `this.remove()`，不加类，退路自动成立。
- **十张图按需加载**：`<img>` 上写的是 `data-src`，`pickScene()` 只在换到某套主题时把那一套的两张补成 `src`，首屏只拉 88~130 KB 而不是 542 KB。这里不能用 `loading="lazy"`：祖先 `display:none` 的图片永远不会开始下载，等它变可见也不会补触发，实测十张里只有一张加载。
- 5 套主题的切换靠 `.scene` 上的 `data-scene` 属性配 CSS 选择器，未选中的整组 `display:none`；随机取值排除上一次的那一套，所以每次切屏都看得见变化。漂浮纸页（共 5 张）和鼠标视差容器放在主题外面，五套共用，切背景时纸页不会跟着消失。
- 顺带修掉一个既有缺陷：两侧远景原本只让出 40px 呼吸位，但纸页用负 `right` 故意探出容器、鼠标视差还能再推 ±17px，实测 901~1100px 之间会压到 560px 正文栏 21~31px。呼吸位改成 100px（`calc(50vw - 380px)`），≥1440px 由 `19vw` 封顶所以桌面观感不变。
- 人眼过了一遍矢量版后重画过三处：第 5 套的亭子白柱在 .45 透明度下几乎看不见（加描边、补栏板）、桥只剩一条细弧线（改成填充的桥身 + 桥面 + 栏杆立柱）、垂柳原本是个箭头形色块（改成树冠 + 下垂枝条）；第 4 套的旗杆原本悬在看台上方 28px，已接到屋脊。换成水彩后这三处只影响兜底图，仍然保留。
- `client/design/README.md`：文件清单与分享注意事项、打开方式、八屏与 `phase` 对照表、`:root` 变量改色对照、动效清单、素材来源与合规提示、已验证与未验证清单。
- 原型文案按真实字量写：事件详情约 350 字中文、3 个选项、结果文案约 120 字，用来暴露排版问题，不是占位符。
- **`client/` 脚手架**：Vite 8 + React 19 + TypeScript 严格模式（`strict` + `noUnusedLocals`/`noUnusedParameters`）。依赖装在 `client/package.json`，仓库根是 npm workspaces（`client` + `server`），根 `node_modules` 统一。`vite.config.ts` 里 `host: true` 是给「局域网设备上完成一次结算」这条验收留的，`/api` 代理到 `http://localhost:3000`（端口取 `.env.example` 的 `PORT`），现在还没人用。
- **样式整体搬进 `client/src/styles/global.css`（11.7 KB）**：原型那份样式**逐字复制、一个字没删**，`:root` 变量、动效、断点全部原样。React 里靠条件渲染天然只挂一屏，`.screen{display:none}` / `.screen.on{display:block;animation:fade .3s}` 这两条改成了给外层一个带 `key` 的 `.screen on` 包装来复用（见验证记录）。改配色仍然只改 `:root`。
- **8 屏拆成 `pages/` + `components/`**：`StartPage` / `GamePage`（①②③④ 四个中间态共用，顶栏一样，只换卡片和底部按钮）/ `EndingPage` / `ErrorPage`；组件按职责分 `Chrome`（顶栏 + 档位 + 历史）、`Cards`（事件卡 / 结果卡 / 等待卡 / 错误卡）、`SceneBackdrop`（两侧远景 + 视差）、`ArtFrame` + `art/`（主视觉与内联 SVG 兜底）、`Photo`（位图加载状态机）、`ProtoBar`。内联 SVG 是从 `design/prototype.html` 机械转成 JSX 的，注释里标了出处，别手改缩进。
- **状态机 `client/src/state/useGame.ts`（170 行）**：调用一律「发完整快照、收完整快照」，所以刷新和重试都不会二次累加。这一层独占 `revision` 比较、过期响应丢弃、localStorage 读写；页面组件只接 props 和回调，就是待协作事项 3 里提给成员 D 的那个接缝，D 接手时只需要把这一层的存储调用换掉。
- **本地假 API `client/src/api/`**：`script.ts` 是 14 天事件脚本（每天 3 个选项、每条 effects 都落在游戏规则已确认的单次增减范围内）+ `advanceOneDay` 等纯函数；`ending.ts` 按已确认阈值表算 A~D 档并出四段文案；`mockApi.ts` 是三个 async 纯函数，带 0.4~1.2 秒可配置延迟、`setFault()` 一次性失败注入、越界数值自检（`EFFECT_OUT_OF_RANGE`）、以及幂等分支（同一个 `eventId` 重复提交不二次结算）。
- **走查条改造成「塞快照 + 注入失败」**：原型里它能直接跳到任意一屏，React 里不行——状态机跑起来之后中间态只能由真实转换到达，硬跳会造出不存在的状态。所以改成每个预设先构造一份合法快照塞进状态机，`hold` 标记决定要不要立刻补接口（①④ 两个态要停住给评审截图）。它只写 localStorage 和状态，不碰产品代码路径，上线删 `App.tsx` 里那一行即可。
- **12 张插画按 Vite 静态资源导入**：`src/sceneArt.ts` 里十行字面量 `new URL('../design/assets/side-N-l.webp', import.meta.url).href`，等价于原型里的按需补 `src`（未选中的主题根本不请求）。**路径必须是字面量**，写成变量 Vite 就静态分析不到、不会把文件发进产物。没有放 `public/`，所以拿得到内容 hash。
- **临时存档 `client/src/state/store.ts`**：键名 `campusmock:snapshot`，读回来先做结构校验，**校验不过时不清空、不改写**，只提示版本对不上（对应「不静默清空存档」那条要求）。文件头写明是给成员 D 的 `src/storage/` 落地后整文件删除的临时件。
- **`Deploy.md`（仓库根）**：安装、`npm run dev:client`、局域网地址、类型检查与构建、8 屏逐屏怎么到达、失败注入的语义、「改哪里」文件对照表、接真后端时要动的三处、常见问题、本轮验收对照。
- **存档写入失败不再假装成功**：`store.ts` 的 `saveSnapshot` 一直会返回 `false`，但 `useGame.ts` 三处调用把返回值全丢了——浏览器拦写入或配额满时界面照常推进，正撞验收项「写入失败…不假装保存成功」。现在写失败置一个 `saveFailed` 布尔，`App.tsx` 在窄栏顶部渲染一条 `role="status"` 的常驻提示（不切屏、不挡操作，下一次写成功自动消失）。
- **14 天剧本的数值—文案一致性审计**：逐条核了 42 个选项的 `effects` 与 `resultText` 是否互相解释，重点是玩家看得见的 `精力`/`金钱`。改掉两处硬伤：第 2 天选项 3「领了宣传单就走，没敢填表」原本与选项 2 效果**完全相同**（社交 +1），既是换皮选项也和"什么都没做"的文案矛盾，改成全 0；第 8 天选项 1 效果是金钱 −60，文案却写「挂了以后卡里多了两百」，玩家会看见数字和句子相反，把文案改成与支出一致的说法。剩余 6 处同类问题（金钱 −30~−70 的支出，在选项文案和结算文案里都找不到出处）**没有自己改**：其中 3 处落在 14 天自动走位路径上，改数字会把终局金钱 740 推过 800 让档位从 B 变 A，得连档位和结局文案一起重算；这批剧本文件头就写明是给成员 C 替换的占位文本，所以把它作为提示词要求交出去（见待协作 17）。
- **走查面板补两个顶格态（2026-09-11）**：`client/src/api/presets.ts` 新增 `longText`（第 5 天·待选择）与 `longEnding`（已结束）。字量不是我随手写的长句，是照 C 的 `server/src/ai/prompts/values.ts` 逐项顶到上限：事件 title 30/30、description 300 字写成三段（段间空行，走 `paragraphs()` 切多个 `<p>`）、三个选项 text 分别 55/54/59 且每个都带第二行动机（走 `splitOption()`）、resultText 115/116/120；结局 description 399/400（四段）、evaluation 194/200、advice 138/150。之前的走查态最长只有二三十字，等于排版从没按真实上限验过。
  两个态都**复用 `day4Settled()` / `fullWalkSnapshot()` 的历史，只换文案不换数值**，因此属性与历史重算仍然一致——`⑥` 能被真实 Express 的 `validateSnapshot` 接纳并正常结算（实测见验证记录），点任一选项就会走真接口进 `③ 展示结果`，长 `resultText` 也在真链路上验到，不必等模型。效果值同时覆盖三种显示规则：`money: 0` → 「没变」、`money: -300` → 规则允许的最大单笔支出、`energy: -2`。走查条本身 `flex-wrap:wrap`，多两个按钮不会撑破窄屏；`global.css` 里文本容器没有 `nowrap`/`line-clamp`/固定 `height`，只有插画框有 `overflow:hidden`，所以超长文字不会被裁掉（静态核对，非截图）。
  代价如实记一条：这批夹具会打进生产包，`npm run build -w client` 的 JS 从 281.98 KB 涨到 285.11 KB（gzip 89.25 → 90.95），+3.13 KB。走查条在生产构建里根本不渲染，但 `presets.ts` 被它静态 import，所以没被摇掉。为省 3 KB 改成动态 import 不值得，记下来备查。
- **正文排版整体改衬线（2026-09-11 深夜，`feat/client-prose-serif`，单文件 +13/−8）**：需求来自两条反馈——「文字看着太单调」和「结局页『评价』『给接下来几周的你』和正文分不开」。方向定成纸质刊物：叙事走衬线，控件与注解留无衬线。改动全在 `client/src/styles/global.css`。
  - `--font-diary` 由 `"Songti SC","STSong","SimSun",Georgia,serif` 换成 `Georgia,"Source Han Serif SC","Noto Serif CJK SC","Songti SC","STSong","SimSun",serif`；新增 `--font-ui` 变量，`body` 从硬编码字体栈改为引用它。
  - `.body` → 衬线 17px / 行高 1.95 / 字距 .02em / 段首缩进 2em。`.body` 全仓库只有 5 个消费点，都是叙事文字（事件详情、结果正文、结局的详情 / 评价 / 建议），所以这次改动不会波及控件。
  - `.opt` 主行跟正文同族 16.5px，第二行动机 `.opt small` 显式留无衬线 12px，形成「叙事 / 注解」两层。
  - `.card .kicker` 12→13px、字距 2→1px、`--ink-faint`→`--ink-soft`，前置 Georgia 让「第 5 天 · 9 月 5 日」里的数字走衬线。
  - `.sec .h` 13px 品牌蓝 → 19px 衬线 `--brand-deep`。

  **两条坑记下来，别被退回时重踩**：① CSS 字体回退是**按字形**匹配的，Georgia 必须排在中文衬线**之前**——`Songti SC` 自带西文与数字字形，排在后面就轮不到它出场，改顺序等于把数字也换成宋体。② Windows 宋体没有真粗体，`font-weight:600` 是浏览器合成的假粗，实际不构成层级区分。`.sec .h` 第一版用 16px + 墨色，与 17px 衬线正文之间字号差和色相差都接近 0，唯一指望的字重又不生效，所以「改了等于没改」；现在层级靠**字号 + 色相**两个量同时起作用，字重只是顺带。


- **玩家档案对接（2026-09-12 凌晨，`feat/client-prose-serif`，6 文件 +158/−15）**：把 C 预留的 `PlayerProfile` 从「有接口没人喂」变成界面上真能填、请求里真带着。四个决定连理由记下来，退回时好谈：
  - **放请求体顶层，不进快照**。`{ requestId, snapshot, profile }`，档案为空时整个键不出现（不发 `null`，省得服务端再判一次）。为什么不塞进 `snapshot`：快照结构已经冻结、`schemaVersion`/`rulesVersion` 有版本校验，为一个装饰性字段升版本不值当；而且档案不进快照就意味着 D 的存档校验器一行都不用改。实测两端校验器（`server/src/game/service.ts` 的 `validateSnapshot`、`client/src/storage/index.ts` 的 `isSnapshot`）都是**逐字段白名单式**，多余字段放行，所以现在这版发过去也不会报错。
  - **只在开局问一次，之后不给改**。C 的提示词把档案写成「玩家固定信息，不得更改、不得杜撰与之矛盾的内容」，而中途改口会让前几天的日记和改后的档案对不上——这属于内容自相矛盾，不是界面问题。有存档时开始页只回显一行「这局记下的你：男 · 计算机科学与技术」，没有输入框。
  - **两项都填才生效**。C 的 `PlayerProfile` 两个字段都是必填，只填一项没法构造合法对象；与其替他补一个默认值（那等于前端替玩家假设性别），不如当成没填、走提示词里那条中性表述分支。界面用一行提示把这条规则说给玩家：「缺任何一项，就当作没认识过你来写」。
  - **性别走「男 / 女 / 不填」三档，专业是自由文本**。三档的原因是 `gender` 会被原样拼进中文句式 `性别 ${gender}`，给自由文本等于让玩家往提示词里塞任意句子；`不填` 只是把值清成空，不是第三个枚举项。专业保留自由文本（学校专业名没法枚举），但限长 20、`\s+` 压成单空格再 `trim`，这样既不可能塞进换行打断固定句式，也限制了注入文本的体量。存回读时同一套归一再过一遍，防手改 localStorage。
  - 实现：新增 `client/src/state/profile.ts`（`PROFILE_KEY='campusmock:profile'` + `normalizeProfile` / `loadProfile` / `saveProfile` / `clearProfile`，档案写不进去不拦开局）；`gameApi.ts` 加 `setProfile`，与既有 `setFault` 同构，所以 `ApiCall` 签名、`dispatch`、`lastCall` 重试重放全部没动；`useGame.ts` 挂载时把本机档案读回适配层（否则刷新后继续玩，结局那次生成就丢了档案），清档与坏档清除时一并清掉；`StartPage.tsx` 用 `<form onSubmit>` 承载，回车即开局。
  - 走查条不需要新预设：档案不是快照的一部分，`presets.ts` 一个字没动。`mockApi` 也没动——离线走查模式下档案不参与生成，这是刻意的，假剧本本来就不接模型。

## 接口与依赖

- 字段名严格照 [接口约定](接口约定.md) 和 [游戏规则](游戏规则.md) 已冻结的部分使用：`title/description/options`、`text/effects/resultText`、`academics/social/energy/money`、`finalAttributes/grades`；React 这轮又按同一份文档用上了快照与响应的外层字段 —— `schemaVersion/rulesVersion/gameId/revision/phase/history/currentEvent/ending`、`requestId/baseRevision/snapshot`、`error{code,message,retryable}`。**没有自造过任何字段**，界面上多出来的日期与星期是前端按 `history.length` 推的（见待确认 12）。**这轮破了这条**：`profile` 是我加的第一个契约外字段，已在待协作 23 里请成员 B 收进契约与 `GenerateEventRequest` / `GenerateEndingRequest`——**类型那一半 B 在 #24 做掉了，文档那一半仍然缺**（`docs/接口约定.md` 里至今没有 `profile` 这一行），所以对这份契约而言它今天仍是契约外字段。除它之外仍然没有任何自造字段。
- **默认已经在打真后端**：`client/src/api/gameApi.ts` 按 `VITE_API_MODE` 分流，不设该变量时 `API_MODE === 'server'`，三个接口走 `/api/...` 相对路径（dev 由 Vite 代理到 3000，生产由 Express 直接托管 `client/dist`）。存档读写归成员 D 的 `client/src/storage/`，我这层不再持有任何存储实现。本地假 API 仍然存在，只在 `VITE_API_MODE=mock` 下生效，`setFault()` 失败注入也只在这条路上有效。
- 假数据仍然是**模块内依赖，不是接口依赖**：`src/api/mockApi.ts` 那三个函数一直是「进一份快照、出一份响应」，所以从假数据切到真接口时页面与状态机一行代码都没动，只是多了一个 `gameApi.ts` 适配层。留着的用处是给离线走查和失败注入用。**今晚这一条要精确化**：`generateEvent` 多了第三个参数 `onFrame?`（假服务也逐帧输出，见待协作 26），`chooseOption` / `generateEnding` 签名未变。因为是可选参数，所有老调用点与返回值形状一律不受影响——但这轮**状态机与界面各动了一点**：`useGame` 多了一个不进存档的临时 `draft`，等待卡多了一个"有草稿就画字、没草稿就画骨架屏"的分支，`App.tsx` / `GamePage.tsx` 只是把这个草稿往下传一行。
- 非接口依赖：`client/design/assets/` 下 12 张 webp，合计 642 KB（两张主视觉 101 KB + 两侧远景 542 KB）。**素材刻意留在 `design/` 没搬进 `src/assets/`**：走查原型和产品代码引用同一批文件，避免两份素材各自漂移。构建时 Vite 照样能把它们发进 `dist/assets/` 并带上内容 hash。
- 产物实测（2026-09-11，含本轮顶格夹具）：JS 285.11 KB（gzip 90.95 KB）、CSS 12.23 KB（gzip 3.67 KB）、12 张 webp 642 KB。首屏只请求当前主题的 2 张侧景，88~130 KB。**档案这轮之后**：JS 286.96 KB（gzip 91.73 KB）、CSS 13.45 KB（gzip 3.94 KB），增量是开始页那两个输入与归一逻辑，衬线那轮已另计在验证记录里。**错误屏拆分这轮之后**：JS 288.16 KB（gzip 92.12 KB）、CSS 不变 13.45 KB（gzip 3.94 KB），1.2 KB 全是两类新文案与第四颗注入按钮。**结算页等待反馈这轮之后**：JS 288.45 KB（gzip 92.19 KB）、CSS 13.93 KB（gzip 4.04 KB），增量 0.3 KB + 0.5 KB，就是流光条那一句与一组样式。**第 14 天结局死锁这轮之后**：JS 288.46 KB（gzip 92.20 KB）、CSS 不变 13.93 KB（gzip 4.04 KB）——只多一个派生布尔与注释，体积几乎不动（+0.01 KB）正是这次改动的目标之一。**傍晚把 `origin/main`（#26）并进来后重新构建，产物文件名 hash 与第 14 天那一轮逐字相同**（`index-DMW56tej.js` / `index-DJVxUdQn.css`）——Vite 的文件名带内容 hash，hash 没变就是"前端一个字节都没变"，#26 的改动全在 `server/`。**流式前端段这轮之后**：JS 290.10 kB（gzip 92.93）、CSS 14.24 kB（gzip 4.12）；这一轮 hash 确实变了（`index-AUQOt78j.js` / `index-n9nGZjek.css`），因为动的正是前端自己——读帧循环、帧解析器、草稿分支和一支笔尖。
- `PORT=3000` 取自 `.env.example`，只用在 Vite 的 `/api` 代理目标上。

## 待协作事项

**给成员 B：**

1. `shared/` 目前只有一个 `.gitkeep`，没有 `package.json`。`Snapshot`/`GameEvent`/`Attributes` 类型放哪、怎么 import（workspace 依赖还是 TS path alias）请定一下。**这条我没等**：已经按接口约定写了一份临时的 `client/src/state/types.ts`（90 行，只有类型和 `isFailure`）。定下来之后我删掉它换成 import，对表时注意两点——`ending` 在 `phase !== 'ended'` 时是 `null` 还是缺字段、`history` 里要不要另存 `day` 字段（我现在按 `history.length` 推天数）。
2. mock 快照语义请复核：`revision` 每次转换递增、`history` 天数从 1 连续、`eventId` 全局不重复、`effects` 四项齐全（未影响写 0 不省略）、以 `history` 长度作为已完成天数而非另存 day 字段。**这轮已经从"原型里按这份语义摆的数据"变成可执行代码**：`client/src/api/script.ts` 的 14 天脚本 + `advanceOneDay` 就是一份能跑的合法快照生成器，你的校验器和测试用例可以直接拿它当 fixture，不用另造数据。

**给成员 D：**

3. 接缝提案：快照由 D 持有，我的页面组件只接 props ——`snapshot` 加 `onChoose/onContinue/onGenerate/onRetry` 回调，组件内部不碰 localStorage、不比较 `revision`、不丢弃过期响应。**这条已经照它实现了，可以直接接**：`useGame.ts` 里页面组件确实只吃 props，`revision` 比较、过期响应丢弃、重试重放、存档读写全在那一层。你接手只需要换三处调用（`saveSnapshot` / `loadSnapshot` / `clearSnapshot`），或者把你的 `src/storage/` 包成同名导出，我这边一行不改。另外定一下键名：我现在用的是 `campusmock:snapshot`，值就是接口约定里那份完整快照的 `JSON.stringify`，没有加版本号外壳——版本判断我放在结构校验里（读回来先看字段齐不齐，不齐就报「版本对不上」且**不清空**）。你要改成 `campusmock:v1:snapshot` 之类的请现在说，越晚改越要写迁移。

**给成员 C：**

4. 三个选项不能出现明显最优解，这条要进提示词。原型里的第 5 天事件是按「三条都合理、各有代价」写的，可以当风格基线。
5. 结局屏的「评价」和「给接下来几周的你」是按验收项「不把低档位等同于个人价值」写的，可以当结局提示词的输出样例。

**待产品/团队确认：**

6. 结局页我按「四维都给 A~D 档位，但只有精力和金钱附精确数字」设计。如果"过程中隐藏学业社交"要延伸到结局也不给数字，我去掉数值。
7. 金钱变化为 0 时我写的是「没变」而不是「0」，避免像表格。可改。
8. **桌面端主视觉已放大（2026-09-10）**：实测是在电脑上浏览，原来 330px 上限的插画在 2000px 宽的屏上只占 16%，观感太小。现在 ≥768px 时开始页插画对称溢出到 660×400、结局横幅加高到 488×240、标题 40px；窄栏刻意保持 560px 不加宽，否则 350 字的事件卡会拉成一行四十多字的长句。剩余待决：要不要进一步做 ≥1024px 的左图右文双栏海报版式（视觉冲击更强，但等于多一套桌面版式）。
9. 窄屏优先的前提要修正：验收项要求「至少一台局域网设备完成事件生成与结算」，但实际演示是在电脑上看的。所以现在是**手机和桌面两端都要成立**，桌面不再是"能看就行"。
10. **AI 角标：这次我先动了，需要你们追认或退回。** 十张两侧水彩在抠图时把右下角的「Qoder AI 生成」角标一并去掉了——它在 .45 透明度的装饰图上仍能看出是一行灰字，而且和「画面里不出文字」的生成约束冲突。开始页与结局页那两张**角标原样保留**。要恢复成带角标的版本，把 `vibe_images/key_side.py`（**在仓库外、未入库**，我本地素材管线目录）里 `KEEP_WATERMARK` 改成 `True` 重跑十张即可，几分钟的事。保留（当作 AI 内容声明）／全部裁掉／换无角标生成，请定一个。
11. 更前置的一条：**比赛是否允许 AI 生成的美术素材进产品代码**。如果不允许，我把这 12 张全部退回内联 SVG 版本（两侧那五套矢量兜底这轮已经画完并复核过，改动量很小，只是观感退回简版）；这条我判断不了，需要看赛题要求。
12. **顶栏的日期与星期是前端推的，接口里没有这两个字段。** 我按「第 1 天 = 9 月 1 日 = 星期六」算（`src/state/calendar.ts`），依据是原型里已确认的「第 5 天 · 星期三」「第 14 天 · 星期五」两点都能对上，也符合 9 月开学。要么团队认下这个起点，要么成员 B 在快照里给一个 `startDate`。顺带一条：原型第 1 天标题旁的「有风」是天气，契约里没有天气字段，**我没有搬过来**，要保留就得加字段。
13. **侧景主题已改成确定值，不需要你们拍板了，只报一下**（2026-09-10 晚）。之前文档里写的是「React 里换成按天取模 `day % 5 + 1`」，而我第一版实现保留了原型的随机换——两条各有毛病：取模会让同一天里的 ①→②→③ 三次换屏背景一动不动（观感退回"没有换场"），随机则让截图无法回溯。现在按 **`(天数 + 屏序号) % 5`** 取，两条同时成立：同一个状态永远同一套图（截图可复现、可写进验收记录），换屏一定换图。实现是 `useGame.ts` 里的纯函数 `sceneFor(day, screen)`，`scene` 不再是 state、也没有那个换屏 effect 了。要退回纯按天，改这一个函数。
14. **不可重试错误屏的「查看运行说明」按钮现在是 `disabled`**，不是漏做：仓库根的 `Deploy.md` 已经有了，但打包后的前端没有任何地址能指向它。要定的是「跳仓库文档（局域网设备上依赖外网和仓库权限）」还是「做一屏内置说明」。
15. **根目录 `npm run build` 现在必然失败**（给成员 B / D）：`server/package.json` 的 `build` 是 `tsc -p tsconfig.json`，但 `server/tsconfig.json` 不存在，而且 `server` 没声明 `express` / `tsx` / `typescript` 任何依赖。`npm run build -w client` 是通的。不修这条，第 5 天的"一键构建 + 运行说明"会卡在 server 上。
16. **走查条演示时要不要留着？** 它不属于产品界面（顶部那条灰栏），但演示时用它跳到第 5 天比真跑 15 秒快得多。默认我留着，正式上线前删 `App.tsx` 里那一行。如果评委要的是"干净的产品界面"，我就改成只在 `import.meta.env.DEV` 下渲染。

**给成员 C（续，编号接全局序列）：**

17. **提示词要加一条硬要求：结算文案必须解释玩家看得见的 `精力` / `金钱` 变化。** 界面在结果卡上只展示这两项的带符号数字（学业、社交刻意不展示），所以玩家一眼就能把句子和数字对上——文案没交代这笔钱花在哪，玩家就看到"莫名其妙少了 50"。这条是审计 42 个选项时发现的，占位剧本里还剩 6 处没修，位置列给你当反面例子（`client/src/api/script.ts`）：第 9 天选项 3 `−30`、第 10 天选项 3 `−50`、第 14 天选项 1 `−50`（这三条不在 14 天自动走位路径上）；第 11 天选项 2 `−70`、第 12 天选项 2 `−50`、第 13 天选项 1 `−45`（这三条在走位路径上）。反向也算一处：第 8 天选项 2 文案写「把购物车那本书结算了」、效果金钱却是 `0`。
    **为什么这 6 处我不自己改**：走位上那三处一共 `−165`，把文案补成解释得通就得同时改数字，终局金钱会从 740 推到 905，跨过 800 让金钱档从 B 变 A——那要连结局文案里"金钱"那句和 `docs` 里的数值链路记录一起重算，属于 C 生成真实剧本后自然消失的问题，不该在占位文本上追第二轮。要落地的话，请让模型在写 `resultText` 时把本次 `effects` 里非 0 的 `energy` / `money` 至少各带一个具体动作（买什么、熬到几点），并**禁止给出 `effects` 里不存在的支出**——反方向同样要禁，第 8 天选项 2 就是现成的例子：文案说把书结算了，效果里金钱一分钱没少。

**已闭环（本轮实测确认，不用再回）：**

- **14** 已由成员 D 做掉：`ErrorPage.tsx` 里「查看处理说明 / 收起处理说明」是内置屏，不再依赖 `Deploy.md` 的可寻址性，那个 `disabled` 按钮已经不存在。
- **15** 已由成员 B 做掉：`server/tsconfig.json` 有了，`express` / `cors` / `tsx` / `typescript` 都进了依赖，根目录 `npm run build` 现在前后端都通（实测见验证记录）。**顺带记一条环境坑，别人也会踩**：我第一次跑失败不是代码问题，是 `npm install` 因为本机 npm 缓存目录没有写权限而**静默没装上** server 的新依赖。判断方法是 `npm config get cache`，如果指向 `D:\Program Files\...` 这种需要管理员权限的路径，用 `npm install --cache "<一个可写目录>"` 装完再构建。
- **16** 已由成员 D 做掉：走查条生产构建默认不渲染，要演示时 `VITE_ENABLE_PROTO_BAR=true`。
- **18** 已由成员 B 做掉（PR #13，合并于 `7ee4b00`）：`controller.ts` 现在从 `../ai/index` 导入 `generateEvent` / `generateEnding` / `loadAiConfig`，`server/src/ai/` 全仓库再无"没人引用"的模块；`toDigestItem()` 负责把 `history` 映射成 C 要的 `HistoryDigestItem`（`chosenText` → `chosenOptionText`）。**但接线完成不等于验收通过**：「事件与结局来自模型输出」「内容多样性与前后承接」这两条仍需配好密钥后端到端真跑一次才算，目前还没有人跑过。前端侧确认不用改任何代码——事件字段结构与 `client/src/state/types.ts` 完全对得上。

**给成员 B / C：**

> 下面 19 这条已整理进独立交接文档 [联调待办-B与C](联调待办-B与C.md)（带可粘贴的复现命令与错误码透传建议），讨论请在那份文件里进行；这里只留摘要，免得两处文字各自漂移。同文档里的「AI 接线」一条（原 18）已由成员 B 做掉，见下方已闭环。

19. **服务端不校验 `revision`。** 我拿一份 `revision: 99` 的旧快照去打 `/api/events/choose`，返回 HTTP 200 并正常结算。"同一事件重复提交"服务端是挡住的（按 `eventId` 命中历史直接回原快照），但服务端无状态、无从知道哪份是最新进度，所以**旧快照会被当作当前状态接受并产出分叉结果**；浏览器路径上目前只有 `useGame.ts` 那五重比对在挡。修法很便宜，见交接文档的方案一。

**给团队：**

20. **两个错误屏现在很难演。** `setFault()` 失败注入只在前端假 API（`VITE_API_MODE=mock`）里生效，默认 `server` 模式下走查面板那两颗按钮是 `disabled` 的。真链路上要复现「可重试」只能把 Express 停掉（会拿到 `NETWORK_ERROR`，实测成立），要复现「不可重试」得让服务端返回 `retryable: false` 的错误码——目前只有喂一份坏存档才能触发。演示时如果临时想截这两屏，`VITE_API_MODE=mock` 起一次前端是最省事的办法，需要有人把这写进运行说明（`Deploy.md` 现在归负责人，我没动）。
21. **上一条依赖的 `VITE_API_MODE` 现在照 `.env.example` 配不出来**（给成员 B / D）。`7ee4b00` 上这份文件只剩 6 个 `DEEPSEEK_*` / `AI_*` 键，`PORT`、`VITE_API_MODE`、`VITE_ENABLE_PROTO_BAR` 三个都不在了，而 `client/vite.config.ts` 的 `envDir` 指向仓库根、`server/src/index.ts` 也从根目录读 `.env` —— 这份文件是前后端共用的唯一入口，删掉的恰好是前端那三个。后果：新同学照模板配出来的前端只能打真后端，一旦没密钥就正好落进上面那条 `AI_CONFIG_ERROR` 死胡同，而离线路径在仓库里查不到怎么开。另外文件首行带 BOM（`U+FEFF`），有人拿脚本读它会多出一个不可见字符。**同条里那个模型名漂移已由 #19 闭环**：`.env.example` 与 `server/src/ai/deepseek.ts` 的默认值现在都是 `deepseek-v4-flash`，不再是我记录时写的 `deepseek-chat`。剩下的两处（三个前端键仍缺、BOM 仍在）还在别人文件里，我只报不动。
22. **衬线字体栈在演示机上不一定成立，需要有人拍板**（给团队）。`--font-diary` 现在依赖 Georgia（Windows / macOS 自带）+ `SimSun`（Windows 中文衬线兜底），整条链在两台主流桌面上都落得下来。但如果演示机是 Linux、精简版系统，或者局域网设备是安卓浏览器，中文衬线会一路掉到末尾的 `serif` 默认值——观感从「纸质刊物」退回系统默认，而且**每台设备不一样，截图不可复现**。三个选项：① 认了这个风险，演示只用 Windows；② 内嵌一个中文衬线子集（只覆盖界面实际用到的字，按 14 天真实文案估计在几百 KB 量级，我没实测过，别照这个数字决策）；③ 退回无衬线，放弃这轮方向。我倾向 ①，但这条判断要看明天到底在几台什么设备上看，你们定了我再动。

**给成员 B（续，编号接全局序列）：**

23. **【2026-09-12 已闭环：成员 B 在 PR #24（合并于 `4bbf241`）做掉了前三小点，我逐处复核过】** `server/src/game/types.ts:67` 与 `:80` 两个请求体都有了 `profile?: PlayerProfile`；`controller.ts:94`（事件）与 `:228`（结局）解构出 `profile`，经 `:122` / `:256` 归一化后在 `:131` / `:267` 透传给 C 的生成函数（**行号已按 #26 合并后的工作树更新**，#26 删掉了模块顶层那块 `aiConfig` 初始化，所以整体上移）。**他多加的那个 `normalizeProfile`（`controller.ts:46`）与我前端那份（`client/src/state/profile.ts:11`）语义完全一致**——都要求 `gender` 恰为「男」/「女」、`major` 非空且 trim，缺任一项返回 `undefined` ⇒ 双侧都判"半个档案 = 没有档案"，走 C 的中性表述分支，不会出现一半生效。**第四小点仍未做**：`docs/接口约定.md` 里没有 `profile` 这一行（#24 只动了 `.env.example` + controller + types 三个文件；**2026-09-12 傍晚在 `69c6d44` 上再核一次，`grep -c profile docs/接口约定.md` 仍是 0**，#26 也没碰那份文档）。现在「契约外字段」已经真的进了服务端请求体类型，文档却没有它，这条归 B / D 补，我不动那份文档。**残余风险一条如实记**：服务端只 trim 不校验长度，上限完全靠我界面那 20 字；`express.json({limit:'1mb'})` 意味着绕过界面直发请求可以把很长的文本塞进提示词。玩家自己的局影响有限，但要收口得在 `normalizeProfile` 里加一行 `slice`。原始诉求保留在下面，供后来人对照。

    前端已经把 `profile` 发出去了，`controller.ts:87` 只解构 `requestId, snapshot`，所以现在这个字段被**静默丢弃**（不报错、也不生效）。改动很小，四处：
    - `server/src/game/types.ts:57` 与 `:67`：`GenerateEventRequest` / `GenerateEndingRequest` 各加 `profile?: PlayerProfile`，类型直接从 C 那里引 —— `import type { PlayerProfile } from '../ai/schema'`，`server/src/ai/index.ts:20` 已经把它 re-export 出来了。
    - `controller.ts:120-125`：`aiGenerateEvent({ day, attributes, history })` → 加一项 `profile`。
    - 结局侧同一形状的那处调用：`aiGenerateEnding({ attributes, grades, history })` → 加 `profile`。
    - `docs/接口约定.md` 里补一行：档案是请求体顶层的可选字段，不进快照、不参与 `revision`。
    几个约定先说清楚，省得来回：**没填时那个键根本不出现**（不是 `null`、不是 `{}`），所以 `body.profile` 原样往下传就行，C 的提示词里「未收集 → 中性表述」分支会自动接住；**前端不用默认值、不猜、不填**，`gender` 只有「男」「女」两种取值、`major` 已被我限到 20 字且不含换行，直接拼进 C 的固定句式 `性别 ${gender}，专业 ${major}` 就是通顺中文。你要更严可以在服务端再校一遍长度——**自由文本进提示词这条残余风险我没法在界面侧消除**，只能限制体量：玩家自己写的话只影响他自己那一局，但如果你想干脆禁掉非中文之类，跟我说，前端改成受控下拉也很快。
    合完这一条，"事件与结局按玩家档案个性化"才算真的成立；在那之前这个 PR 的前端部分只是把数据送到门口。

24. **【2026-09-12 傍晚已闭环：负责人在 PR #26（合并于 `69c6d44`）把透传改回来了】** 合并后的工作树里 `server/src/game/controller.ts:86` 已经是 `sendError(res, requestId, code, err.message, err.retryable)`（正是 #19 原来的形状），而那两个写死英文的配置预检随着 `loadAiConfig()` 挪回请求阶段一起被**整块删掉**了（见待协作 27）——所以成员 C 玩家化的那四句文案（`deepseek.ts:37-40`：「心神不定，不知如何落笔」/「落笔前想的太多，有些出神了（超时）」/「回忆有些模糊，可能得再仔细想想（网络错误）」/「写着写着，笔没了墨」）现在能原样出现在界面上，`docs/接口约定.md:50`「提示用简体中文」这一条在 AI 路径上成立。**只剩一个尾巴，很短**：`:89` 未知错误仍发 `'AI generation failed'`（英文），触发条件是抛出的不是 `AiError`，真链路上罕见到基本碰不到，要不要顺手换中文由 B / 负责人定。下面两段是原始记录，保留供对照。

    成员 C 在 `server/src/ai/deepseek.ts:37` 抛的是「缺少环境变量 `DEEPSEEK_API_KEY`，请在后端 `.env` 中配置（密钥不进入前端与存档）」，`:102` 那句是「模型服务鉴权失败，请检查 API 密钥与账户状态」；#19 把 `controller.ts` 的 AI 分支改成透传 `err.message` 之后，这两句会**原样出现在玩家界面上**（`ErrorCard` 直接渲染 `error.message`，我没加工）。方向我不替你们定，两个选项：① C 把 message 改成玩家能懂的一句，调试信息走 `console` 或日志；② 保持透传，B 在 controller 里给 `AI_CONFIG` 单独覆一句玩家文案、原文写进日志。我这边已经把这一类错误的**屏幕其余部分**改对了（见待协作下方的第五件事与验证记录：主按钮「先回到上一页」＋一句不误导的说明），所以不管你们选哪个，界面不用再动。演示现场如果有人故意拔密钥看这一屏，观众会看到 `.env` 这个词——先说一声，免得当成 bug。

25. **【2026-09-12 傍晚状态更新：修复仍然只在本地，主干照旧死锁】** PR #25 被合并时**不含**这个修复——修好的那个提交 `f89a90b` 当时卡在"已提交未推送"（本机 → GitHub 连续七次连不上），#25 里只有流光条那两个提交。刚又拉了一次 `origin/main`（现在到 `69c6d44`）复核：`GamePage.tsx` 仍是 `phase === 'showResult'` 一格、`pendingEnding` 无条件出骨架屏，服务端第 14 天仍返回 `pendingEnding`（#26 合并后位置在 `controller.ts:206-207`）。**所以这条现在仍是未修的线上缺陷，需要另开一张 PR**（#25 已合并、那张分支上不再挂着开放 PR）。改法与下面记的一致，代码已经在 `f89a90b` 里，合并 `origin/main` 后重跑渲染矩阵 18/18 通过、产物 hash 与合并前逐字相同（#26 没有动 `client/` 任何文件）。下面是当时写的闭环记录。

    **【2026-09-12 下午已修，B 侧一行都不用改】** 拉完最新 `main` 重读了一遍调用链，发现我原来记的根因**只在渲染层，请求层是好的**：`continueDay()` 本来就会在 `history.length >= TOTAL_DAYS` 时派发 `generateEnding`（`useGame.ts:256-258`），缺的只是"没有任何控件能让玩家点到它"——第 14 次 `choose` 回来的 phase 是 `pendingEnding`，而 `GamePage` 在这个 phase 只渲染那张没有按钮的骨架屏。所以我列的两个选项都不必用，改法是第三种：**结算那一屏的门从 `phase === 'showResult'` 放宽成 `phase === 'showResult' || (phase === 'pendingEnding' && !waiting)`**（`GamePage.tsx:26`，两个分支共用这一个派生布尔），骨架屏加上 `&& waiting`（`:73`）。三个好处：① 第 14 篇的结算文案保住了（选项 ① 的代价）；② B 的 `controller.ts:199-200` 不必动（选项 ② 的代价）；③ **刷新路径一并修好**——挂载恢复不 `stepFrom`，但 `pendingEnding + 没在飞` 现在渲染的就是那颗「去写结尾」，玩家自己就能踢出去，不再需要退回开始页绕路。顺带把 `dismissError()` 里第五件事那条特例收窄了（`useGame.ts:274-281`）：`pendingEvent` 仍然退回开始页（那一态的界面确实一个控件都没有），`pendingEnding` 不再需要这个特例。**代价一条如实记**：走查面板 `④ 待生成结局`（`hold:true`、零网络）现在显示的是「第 14 篇结算 + 去写结尾」而不是干等的骨架屏，那张骨架屏从此只在结尾请求真的在飞时出现——这本来就是它在真实产品里唯一会出现的时刻。评审若要把骨架屏按住截图，现在得点一次「去写结尾」（server 模式十几秒，够截；mock 只有 0.4~1.2 秒）。如果这条真的挡了走查，我可以在走查条加一颗强制 `waiting` 的开关（约 3 行、dev-only），**先不做，等评审说需要**。原始记录保留在下面。

    第 14 天选完选项会永久卡在「正在为你写结尾」，两种模式都卡。证据链：`server/src/game/controller.ts:199-200` 让第 14 次 `choose` 的返回 phase **直接是 `pendingEnding`**（不是 `showResult`），mock 侧同构（`client/src/api/script.ts:302`）；而客户端 `dispatch` 的成功路径只做 `persistAndApply(result.snapshot)`，`resume` 默认 `'none'`；`GamePage.tsx` 在 `pendingEnding` 只渲染 `EndingWaitingCard`，那张卡上唯一那颗按钮是 `disabled` 的（`Cards.tsx:47`）。于是玩家点完第 14 天的选项就停在一个**没有请求在飞、也没有按钮可点**的骨架屏上，刷新也救不回来，只有退回开始页点「继续上次的日记」才会通过 `resume()` → `stepFrom()` 踢出去。之前一直没暴露是因为走查面板的「跑完整 14 天」是 `runFullWalk` 直接 `await dispatch(..., generateEnding)`（`useGame.ts:303`），绕过了 UI。当时列的两种修法：① 我在客户端补一次 `stepFrom`（代价是玩家看不到第 14 篇的结算文案）；② B 把第 14 天的 `choose` 改回返回 `showResult`。**我和 B 对"谁来触发结局"的理解本来就不一致，这条不是单方面写错**——最终采用的第三种改法（渲染层放宽结算屏的门）两边都不用动文件。
26. **【2026-09-12 傍晚复核：流式效果仍未实现，四处独立证据】** 拉完 `69c6d44` 又查了一遍：① `git grep generateEventStream` 在 `server/src/game`、`server/src/routes`、`server/src/index.ts`、整个 `client/` **零命中**，只有 `ai/` 内部、`ai/README.md` 和它自己的测试引用；② 全仓搜 `res.write` / `flushHeaders` / `x-ndjson` **零命中**，服务端只有 `deepseek.ts:83` / `:175` 两处 `Content-Type: application/json`，即响应仍然是一次写完；③ 客户端搜 `TextDecoder` / `getReader` / `ReadableStream` / `EventSource` **零命中**，连消费的代码都不存在；④ #26 只动了 server 与 CI，`client/` 一行未改。**结论：现在玩家看到的还是"转圈十几秒 → 整段出现"**，`ai/` 里那套底座是死代码。归属也要改一句：不是"只剩 B 的 controller 一处"，而是**服务端调用点 + 传输 + 前端消费三段都没接**，其中前端那一段随时可以做（我这侧）。想要这条在演示前落地，需要一个明确决定，因为三段一起动是今天做不完的量；不做的话今天这条就是「按计划搁置」而不是「坏掉了」。以下是 C 已经做完的部分与原约定。**（今晚这条已经改了一半：前端消费段做完、帧约定换版，见本节最后一段；上面那四处证据里"客户端零命中"那一条从本次提交起不再成立。）**

    成员 C 在 PR #23（合并于 `21ee89d`）做完了我这份约定里"服务端生成侧要做的事"：`chatJSONStream`（`deepseek.ts:155`，请求体 `stream:true` 在 `:188`，SSE 帧缓冲与切分在 `:244` 起）、`generateEventStream`（`generateEvent.ts:71`，返回值与非流式版完全一致、重试语义一致）、以及**我原说归我写的那一段——从残缺 JSON 里提取 `title` / `description` 的解析器 `createEventStreamParser` + `checkStreamViolation`（`streamParse.ts:57`，`ai/index.ts:12` re-export），C 直接写完还配了离线夹具 `src/ai/scripts/stream-parse-test.ts`，归属就此改掉，我不再重复实现**。`ai/README.md:130-132` 甚至已经写好调用样例。附带那条仍然成立、只改行号：`chatJSONWithRetry` 现在在 `deepseek.ts:279-298`，**循环体里仍然一行日志都没有**（C 只在 `chatJSON` 内部按错误类型打了 `console.warn`，第几次重试、为什么重试依然看不见），加一行只要 30 秒，这条比流式更该先做。原始约定保留在下面。

    **今晚更新（2026-09-12 晚）：前端消费段已实现，帧约定按成员 C 已入库的回调形状定稿——请以此版为准，我傍晚写的那版"转发上游原始片段"作废。** 作废理由：C 的 `createEventStreamParser`（`streamParse.ts:57`）已经把残缺 JSON 解析成语义回调了（`onTitle` 标题闭合整体一次 / `onDescriptionDelta` 正文增量 / `onOptionText` 选项整条弹出 / `onResultTextDelta` / `onRetry(attempt)`），服务端再往下转发生 token 等于把他做完的活退回浏览器解析第二遍。**只剩 B 的 controller + 传输那一段，前端不用再动**。约定与实现逐条如下：

    - 传输：`POST /api/events/generate` 的响应 `Content-Type: application/x-ndjson`，一行一个 JSON 帧，**行末必须带 `\n`**（前端按换行切，最后一行没有换行也能吃到，但别依赖）。`server/src/index.ts` 现在没有 `compression` 中间件，`res.write` 不会被缓冲；哪天加了压缩，这条要重新验。
    - 中间帧：`{"k":"title","v":"整段标题"}`、`{"k":"desc","v":"正文增量"}`、`{"k":"opt","i":0,"v":"整条选项文本"}`、`{"k":"reset"}`（对应 `onRetry`：服务端要重问一次，前端抹掉已显示的字）。**`v` 必须是字符串**，非字符串的帧前端直接忽略，不会污染草稿。`opt` 眼下界面不画（选项要等整份结果才可点），但帧是认得的，以后想画加一个 case 即可。
    - **`resultText` 那两帧（B 可以直接不发）**：C 的 `EventStreamHandlers` 一共 **6 个回调**，除了上面四个还有 `onResultTextDelta(index, delta)` 与 `onResultText(index, text)`（`server/src/ai/generateEvent.ts:35-42`，我原稿只列了 4 个，漏了这两个）。要发就用 `{"k":"rtext","i":0,"v":"增量"}` 与 `{"k":"rtextEnd","i":0,"v":"完整"}`；**前端今天两种都收下但不画**，因为结果叙述是玩家选完选项之后才出现在结算页的，而那时终帧早已到齐、界面渲染的是快照里那份完整数据，逐帧显示没有落点。所以 B 侧最省事的做法是**这两个回调直接不接**，不影响任何观感。
    - 终帧：`{"k":"end", …今天那个完整响应体原样…}`，成功就是 `{requestId, baseRevision, snapshot}`、失败就是 `{requestId, error{code,message,retryable}}`，一个字段都不改（键名从我傍晚写的 `done` 改成了 `end`，那版没人实现过，改名无成本）。**只有终帧会写存档**，五要素比对、幂等、重试语义全部原样保留。
    - 前端两种响应都吃：`content-type` 含 `application/json` 就走今天的路子（今天线上就是这个），含 `application/x-ndjson` 才逐帧读；**未知 `k` 一律忽略**，所以上面那两帧 `rtext` / `rtextEnd` 发与不发都不会把界面弄坏，前端也不需要为它先动一行。断流且没有终帧 → 落进既有的「联系不上服务、进度没变、可重试」那一屏。
    - 给 B 的落地量：`controller.ts` 生成事件那一路把 `generateEvent(...)` 换成 `generateEventStream(input, cfg, handlers)`，`handlers` 里每个回调 `res.write(JSON.stringify(frame) + '\n')`，函数返回后照旧 `res.json(...)` 写成终帧（改成 `res.write(JSON.stringify({k:'end', ...data}) + '\n'); res.end()`）。**要先发 `Content-Type: application/x-ndjson` 状态头再写帧**（`res.writeHead(200, {...}); res.flushHeaders()`），否则第一帧到达时浏览器还拿不到类型、前端会按 JSON 路径去读整包然后失败。`generateEnding` 那一路 C 没有流式版，保持原样。
    - **CI 要跟着改一条**：`server/tests/ai-integration.cjs` 与 `routes.test.*` 现在断言的是整包 JSON；接口一旦改成 NDJSON，那些 `res.json()` 读法会当场红。这条必须和 B 的改动同一张 PR，不能分两次——分两次中间那次 CI 就是坏的。
    - 现状判定不变：**"界面上看得到字一点点长出来"这件事今天还不成立**，因为服务端那两段没接。我在 `mockApi` 里让离线假服务也逐帧输出，所以在 `VITE_API_MODE=mock` 的 dev 下现在就能看到效果，但那证明的是前端消费正确，不是真链路通了。

27. **【2026-09-12 傍晚已闭环：负责人在 PR #26（合并于 `69c6d44`）一次修了两层，比我给的方案更稳】** 两件事一起做掉了：① 新增 `server/src/env.ts`，用 `path.resolve(__dirname, '../../.env')` 定位配置（`:7-9`），**不再依赖启动目录**，只在根目录那份不存在时才回退 `server/.env`，且保留外部注入的同名变量优先；`index.ts:1` 改成 `import './env'` 并删掉了原来那段模块体里的 `process.loadEnvFile` 循环——正好堵死我记的那条"回退代码跑在 import 之后"的因果；② `controller.ts` 模块顶层那块 `aiConfig` 初始化（连同 `!aiConfig` 的英文预检）被删除，改成每个请求在 `try` 里 `const aiConfig = loadAiConfig()`（`:125` 与 `:259`）。**所以我原来给的绕法（从仓库根跑 `npx.cmd tsx watch server/src/index.ts`）现在不需要了**，`npm run dev:server` 与 `npm start` 都恢复正常，改 `.env` 后重启即可生效（`tsx watch` 会自己重启，`npm start` 要先重新构建——`README.md` 这轮已经把这段写进"若配置后仍不能调用"了）。顺带一条呈现上的变化：缺 key 时现在抛的是 `AiError("AI_CONFIG", "心神不定，不知如何落笔（未配置API密钥）", false)`，经 `:86` 透传，**玩家界面上看到的是成员 C 那句中文**，不再是 `AI config not loaded, check DEEPSEEK_API_KEY`——待协作 24 与这条本来就是同一个故障的两面。原始报告保留在下面，供后来人对照这段因果为什么这么绕。

    给成员 B：`loadAiConfig()` 回到模块加载期，是 #19 的回归，会当场打死演示（2026-09-12 下午发现，只报不动——文件是你的）。现状：`controller.ts:34-40` 在模块顶层 `let aiConfig = null` + `try { aiConfig = loadAiConfig() }` 里 catch 掉异常只打一行 warn。历史可查：`3304c84`（#13）加上 → `e6fa827`（#19「修复接口联调的问题」）拿掉 → `0eb06bc`（#24「支持 PlayerProfile」）又加回来。**为什么这一行在 dev 与生产上都必然失败**：ES 模块的 import 全部先于模块体求值。`server/package.json` 的 dev 是 `tsx watch src/index.ts`，`npm run dev:server` 的 cwd 是 `server/`；`index.ts:1` 的 `import 'dotenv/config'` 只在 cwd 找 `.env`（`server/.env` 不存在，密钥在仓库根），所以它什么都没加载；而当时 `index.ts:9-14` 那个 `../.env` 回退是**模块体代码**，跑在它前面的 `import routes` → `controller` 早就求值完了 —— **`loadAiConfig()` 永远在 `DEEPSEEK_API_KEY` 进 `process.env` 之前执行**，`aiConfig` 恒为 `null` 且没有任何恢复路径，于是每一次生成事件与生成结局都返回 `AI_CONFIG_ERROR` + `retryable: false`。**5 秒判别法**：看后端启动日志有没有 `[AI] Failed to load config`（这两行日志现在也随着那块一起删掉了）。

## 验证记录

- 验证方式：把原型装进固定宽度的同源 iframe，在 390 / 768 / 1280 三档下逐屏切换并用 `getBoundingClientRect()` 量测；本轮改为主页面直接量测 + 截图复核。
- 结果：8 屏 × 3 档均无横向溢出（`scrollWidth > 视口宽` 全为 false，无元素越界）。
- 结果：关掉「设计标注」后逐屏断言可见文字 —— 除结局页外 7 屏均不含「学业」「社交」字样；「待选择」屏不含任何带符号数字，「展示结果」屏只有精力 `−2`，「待生成结局」屏只有精力 `−3`。选择前不泄露效果成立。
- 踩坑记录：这条断言必须先关掉设计标注。标注框里写着「学业 +3、社交 −1 已写入 history，但界面上不展示」，开着标注跑断言会把说明文字当成 UI 文字，误判成泄露。
- 结果：两张 webp 均加载成功（`naturalWidth` 990 / 1040）；把 `src` 改成不存在的路径后，`onerror` 生效、`<img>` 从 DOM 移除、下层 SVG 可见，四层结构顺序为 svg → img → 光晕 → 落叶。
- 结果：结局页横幅 375×180 固定不随视口变化，与正文间距 22px，重叠缺陷已消失。
- 结果（两侧远景 5 套主题）：在 1440 档同源 iframe 里逐套切换量测，5 套全部渲染出 2 组 SVG；48 次切屏随机取样覆盖 1~5 全部主题、连续重复 0 次；淡入动画触发并在 1 秒后回到 `opacity:1`；同一时刻只有 1 套可见、其余 4 套 `display:none`。
- 结果（净空）：把鼠标视差推到最坏值（左组 `--mx=.5`、右组 `--mx=-.5`）后，在 901 / 950 / 1024 / 1100 / 1280 / 1440 / 1920 七档量测，两侧远景外缘（含探出的纸页）与 560px 正文栏的净空全为正数，最小 32px（901 档左侧）、最大 224px（1920 档）；各档 `scrollWidth` 均不超过视口宽；900 / 880 / 390 三档整组 `display:none` 生效。改前同一量测在 901~1100 段是 −21~−31px，即装饰压到正文上。
- 探针又踩一个坑，记下来：判"哪些元素真的在渲染"不能只看元素自己的 `getComputedStyle().display`。藏在 `display:none` 父级里的子元素，自身 computed display 仍然是 `block`，但 `getBoundingClientRect()` 返回全 0，于是"右组最小左边缘"被算成恒定的 0，量出来的空隙全是假的。要么往上逐层查祖先的 display，要么直接用 `getClientRects().length` 过滤。
- 已人眼确认（矢量版那轮）：5 套主题逐套截图看过，第 4、5 套的三处图形缺陷按「已完成内容」里记的方案重画后复核通过；1440 档「待选择」屏带第 5 套背景的整屏截图确认两侧图元落在留白里、不压卡片。
- 验证方式（水彩版这轮）：走查面板处于隐藏状态时浏览器工具截不了图（`visibilityState=hidden`），改用 headless Edge 出图 —— `msedge --headless=new --allow-file-access-from-files --window-size=1440,900 --virtual-time-budget=6000 --screenshot=...`，配一个临时 wrapper 页把原型装进 1440px 同源 iframe、按 `#n` 强制第 n 套主题并等图片 `complete`，量布局仍然用同源 iframe 直接读 rect。wrapper 是临时文件，验证完已删。
- 结果（水彩版 5 套主题）：200 次切屏后十张 `data-src` 全部补齐，`complete && naturalWidth` 10/10、`has-art` 10/10、兜底 SVG 全部 `display:none`；把 `src` 换成不存在的路径后 `<img>` 自行移除且不会加上 `has-art`，手动摘掉 `has-art` 时兜底 SVG 由 `none` 回到 `block`，掉图退路成立。
- 结果（净空，按位图的新高度重测）：901 / 1000 / 1100 / 1280 / 1440 / 1728 / 1920 七档 × 五套主题，左组推到 `--mx=.5`、右组推到 `--mx=-.5`，外缘（含探出的纸页）与 560px 正文栏的净空全为正，左最小 36px、右最小 29px；各档 `scrollWidth` 均不超过视口宽；390 / 760 / 860 / 900 四档整组 `display:none`。
- 修掉的缺陷：`loading="lazy"` 让十张里只有一张真正下载——祖先 `display:none` 的图片不会开始加载，等它变可见也不会补触发。换成 `data-src` + `pickScene()` 按需补 `src` 后十张全通，顺带把首屏体积从 542 KB 压到 88~130 KB。
- 抠图管线两个坑：① `mix-blend-mode:multiply` 去不掉水彩的白底，因为 `.scene` 是 `position:fixed` + `z-index:0` 自成层叠上下文，混合只在这层内部发生、够不到 body 的渐变，所以只能走真透明通道；② `getbbox()` 把 alpha>0 的抠图噪点也算进内容，实测主体只占画面 24%、落到留白里只剩 125px 宽，要先按阈值过滤 alpha 再裁。
- 角标位置不固定：八张在画布角落、整块矩形就能抹掉，但第 4 套左与第 5 套右的角标落在画面中部偏右下，扩大公共矩形会连水彩一起挖穿。第一次凭目测估的框把看台阴影挖掉一块而字还在，最后靠 1:1 网格截图读出像素坐标、按文件单独给框才干净。教训：估坐标不如画网格读网格。
- 只有量尺寸才发现的缺陷：纸页是 `<span>`，原本靠 `position:absolute` 才自动变成块级；为了把视差和漂移分到两层而改成 `position:static` 后，它退回内联元素，而 `aspect-ratio` 对非替换内联元素无效，高度直接塌成 0 —— 补 `display:block` 解决。教训：给 `<span>` 换定位方式时要顺手确认它还是不是块级。
- 探针自身踩的坑记两条，避免下次误判：① 算居中偏移要减 `innerWidth/2` 而不是 `innerWidth`；② 判"装饰是否压到正文"要分左右两组分别比，把两侧元素混进同一个 `Math.max` 会得出恒定 true 的假阳性。
- 已人眼确认：开始页与结局页截图各一张，插画与页面冷色背景、暖色落日光晕协调，落叶与推近动效在位。
- 验证方式（React 这轮）：`npm run typecheck -w client` 干净；`npm run build -w client` 通过（JS 280.81 KB / gzip 89.27 KB、CSS 11.90 KB、12 张 webp 全带内容 hash 进产物）；dev server 起在 5173 并打印局域网地址（`host: true`）。
- 结果（8 屏逐屏）：②③⑤ 与 errRetry 逐屏截图核对，与原型一致；本轮补齐 ① 与 errFatal —— ① 停在「台灯已经打开了，日记还在路上……」的骨架 + 转圈，errFatal 出「这本日记我读不懂」+ 禁用的「查看运行说明」+ 刻意不给「重新开始」。
- 结果（数值链路，最重要的一条）：假 API 的 14 天走位是**从原型上画好的数字反推**出来的，逐屏对上了：第 5 天顶栏 精力 5 / 金钱 940 → 选完 精力 `−2` / 金钱「没变」→ 终局 `{学业 15, 社交 −3, 精力 −2, 金钱 740}` = **A/D/D/B**，和结局文案里「学业的 A 不是天赋…精力和社交同时落到 D」那句一致。这条同时把阈值表、「允许负数不钳制」、「0 变化显示没变」三条规则跑通在同一个用例上。
- 结果（存档恢复）：跑完 14 天后刷新，走查条显示 `14/14 · 精力 -2 · 金钱 740 · rev 15` 的存档被读回（走查条是开发读数，用的是 ASCII 连字符；界面上的数值才走 U+2212 的 `formatValue`），开始页出现「继续上次的日记」；点「清掉本机存档」后回到 `start`、`localStorage.getItem('campusmock:snapshot')` 为 `null`。
- 结果（桌面 1440）：headless Edge 出图核对开始页 —— 主视觉 660×400 在位、两侧水彩各落在左右留白、云与漂浮纸页在位、560px 正文栏没被压。
- 结果（选择前不泄露）：① 屏可见文字断言 `学业`/`社交` 为 false、无带符号数字。React 侧另外按挂载位置复核了同一条规则：带符号数字只可能出自 `ResultCard` 里的 `DeltaRow`，档位字样只出自只挂在结局页的 `Grades`。
- **修掉的三个移植期缺陷**：① `Photo` 的守卫写成「非 loading 就不渲染」，结果**位图加载成功反而被卸载**、只剩矢量兜底（网络面板里 webp 是 200，页面上却是平的），改成只在 `missing` 时不渲染；② `mockApi` 的 `ok()` 把 `baseRevision` 取成了**已经推进之后**的那份快照，于是每一次转换都被客户端判成过期响应（症状：点两下就报「这一步的响应回来晚了，我没有采纳它」），改成 `ok(base, next)`，六个调用点一起修；③ 移植后 `.screen{display:none}` / `.screen.on{animation:fade .3s}` 成了死 CSS，**换屏 0.3s 淡入丢了**，给页面切换加一个 `key={screen}` 的 `.screen on` 包装复用原规则，实测 computed `animationName: fade`、`animationDuration: 0.3s`。
- 记一条写法坑：`actions` 里 `restart() { this.start() }` 在页面组件把方法解构出来时 `this` 是 undefined。改成把 `startGame` 提成 `useCallback`，`start` 和 `restart` 指向同一个引用。
- 一个刻意的选择，别当 bug 报：注入的「不可重试」失败**复用了真实 code `SNAPSHOT_VERSION_UNSUPPORTED` 和它的文案**，而不是编一个 `MOCK_FATAL`。这样评审看到的错误屏，和玩家真遇到存档版本不兼容时看到的是同一屏。
- 环境坑两条：① 浏览器面板视口窄于 900px 时两侧远景整组 `display:none`，**桌面观感只能靠 headless Edge 出图核对**，别拿面板截图当桌面结论；② 改 `App.tsx` 触发 HMR 时，`mockApi` 模块级的 `fault` 变量不会重置而走查条 state 可能被保留，两边会漂移——失败注入"看起来不生效"时先点一次「不注入失败」。
- 结果（存档写不进去）：在控制台把 `Storage.prototype.setItem` 打桩成抛异常后正常玩，界面顶部出现「这台设备现在写不进存档……」的常驻条、`role="status"` 可被读屏拿到，游戏**还能继续推进**（刻意不切错误屏，一局不该因为写不进本机而作废）；换回真实 `setItem` 后做一次转换，提示条自己消失。反向也测了：全程不打桩时页面上没有这条。
- 结果（四阶段刷新恢复）：分别在 ① 待生成事件、② 待选择、③ 展示结果、④ 待生成结局 四个态各刷新一次，每次都回到**同一个态的同一天**，`history` 长度不变、没有跳天也没有重复结算；② 刷新后仍停在未选择的三条选项前，选择前的泄露断言依旧成立。
- 结果（重复提交）：同一个选项连点三次只结算一次——走查条 `rev` 只 +1、`history` 只多一篇，第二次第三次被 `busy` 挡住。这条对应验收项「同一事件只结算一次」。
- 结果（结局阶段失败后不重做第 14 天）：走到 ④ 之前注入「下次请求·可重试」并刷新，页面回到可重试错误屏，此时读存档仍是 `rev 14 / pendingEnding`（**快照没被改动、第 14 天没被重做**）；点「重试这一步」才推进到 `rev 15 / ended`。
- 验证方式（2026-09-11 真接口这轮）：`npm run dev:server` 起 Express + `npm run dev:client` 起 Vite，用一个临时 Node 脚本**照客户端自己的规则**逐日打三个接口（每次生成/结算都发完整快照并携带新生成的 `requestId`），断言响应契约；脚本是临时件、未入库。
- 结果（14 天端到端）：14 次生成 + 14 次结算 + 1 次结局全通，`currentEvent.day` 依次为 1…14 无重复无跳号，终局 `phase=ended`、`revision=29`、`history` 14 条。每次响应都满足 `requestId` 原样回显、`baseRevision === 请求快照.revision`、`gameId` 不变——我这边五重比对的接纳条件在真链路上逐条成立。
- 结果（属性一致性）：终局服务端声明 `{学业 8, 社交 10, 精力 8, 金钱 750}` 与我按 `initial + Σ history.effects` 重算的结果逐字段相等，"服务端算的"和"客户端能复核的"是同一本账。
- 结果（存档体积，验收项"200 KB 目标"的实测数）：走完 14 天含结局的那份快照 `JSON.stringify` 后是 **5538 字节**，约为 200 KB 的 **1/37**。按模型顶格字量再估一遍最坏情况：14 篇 × (title 30 + chosen 60 + result 120) + 顶格结局 ≈ 5 KB 汉字，UTF-8 三字节约 15 KB，仍不到目标的 8%。localStorage 这条不是瓶颈。
- 结果（异常与重复操作，界面要呈现的分支）：不存在的 `optionId` → `OPTION_NOT_FOUND`；缺参数 → `MISSING_PARAMS`；结构坏的快照 → `INVALID_SNAPSHOT`；`ended` 后再要事件 → `STAGE_MISMATCH`；没走完 14 天就要结局 → `GAME_NOT_FINISHED`。**五者全部 HTTP 400 且 `retryable: false`**，即错误码/文案/可重试标志三元组在真服务端齐备，我的错误屏按 `error.code` 分支不会拿到 undefined。同一事件重复提交走的是幂等分支（HTTP 200、原快照返回、不二次结算），符合"同一事件只结算一次"。
- 结果（负数不钳制，补上之前那条"未实测"）：连续 14 天偏向选消耗精力的选项后服务端返回 `精力 −7`、`学业 28`——服务端只做累加、不设上下界。界面上限由此确定：`金钱` 最坏 1000 ± 300×14，`精力` 最坏 7 ± 2×14。顶栏两个 pill 在 390px 下按最宽取值（`精力 −21` + `金钱 −3200`）估宽约 222px，仍在正文栏 560/390 档可用宽度内，不会挤成两行；数值渲染走 `formatValue`，负号 U+2212 与结果卡同一条路径。成员 C 的提示词里 `attributes.money < 0` 是**预期状态**（还有专门的"金钱已为负"情境提示），所以负数不是 bug，界面按显示处理是对的。
- 结果（顶格夹具合法性）：`⑥ 顶格长文案` 那份快照喂给真实 Express 结算 → HTTP 200、`phase=showResult`、`history` 4→5、属性按所选选项正确累加 `{学业 1, 社交 0, 精力 4, 金钱 640}`；`isSnapshot()` 对 `⑥⑦` 两态均为 true。字量逐项量过：事件 title 30/30、description 300 字内三段、选项 text 55/54/59（每个都带第二行）、resultText 115/116/120，结局 399/400 四段、evaluation 194/200、advice 138/150，无一项越界。
- 验证方式（本轮构建）：`npm run typecheck -w client` 干净；`npm run build -w client` 通过；根目录 `npm run build` 前后端都通过（上一轮记的失败已复现并确认为依赖没装上，见待协作 15 的环境坑）。
- **本轮如实记一条局限**：浏览器自动化仍被本机权限策略拦着，`⑥⑦` 两态**我只做到"数据合法 + 静态核对样式不会被裁切"，没有截图确认观感**。窄屏 390 与桌面 1440 下顶格文案的实际折行、结局页四段长文是否过长需要人眼过一遍——起 dev 后点走查面板的 `⑥ 顶格长文案` / `⑦ 顶格结局` 即可，各一次点击。
- 结果（2026-09-11 晚，在 `7ee4b00` 上复核）：根目录 `npm run build` 前后端仍然全通。**前提是依赖按锁文件装全**——`dotenv` 是 #13 新加进来的依赖，`node_modules` 没重装就会在 `server/src/index.ts:1` 报 `Cannot find module 'dotenv/config'`，这是环境不是代码，别照着它去改启动方式。另外实机核对了一次网络失败路径：`server` 模式下后端没起来时点「开始」，界面停在 `errRetry`，出「今天的日记还没写完 / 暂时联系不上服务，当前进度没有改变」+「再试一次」+「先回到上一页」+ 底部「重试不会消耗这一天，也不会重复结算」，走查条读数仍是 `已写 0/14 篇 · rev 0` —— 即"请求失败不推进进度"这条在真链路上成立，且这条路径与 `AI_CONFIG_ERROR`（服务端起来了但没配密钥）呈现的是同一屏。
- 结果（字体这轮）：`npm run build -w client` 通过，CSS 12.54 kB / gzip 3.75 kB（改前 12.23 / 3.67）；把 `origin/main` 的 #17、#19 两次合进来后，根目录 `npm run build` 前后端各再跑通一次。观感在本机桌面档由人眼过了 `② 待选择`、`⑤ 已结束`、`⑦ 顶格结局` 三屏，反馈「效果可以」。**未做**：390 窄屏截图回归——段首缩进 2em 与行高 1.95 的实际折行只做了静态推算（350px 内容宽减 34px 缩进，约合每行 18~19 个汉字），没有留档。
- 方法记录（比读代码可靠的一条，值得复用）：**判构建产物到底跑哪种 API 模式，去产物里搜接口路径字符串**。`import.meta.env.VITE_*` 是构建期烘进去的，`mock` 分支一旦生效，`post('/api/events/generate')` 会被整条摇掉。今晚实测：mock 模式的产物搜得到假剧本文字、搜不到三个 `/api/...` 路径；改回 server 重新构建后 `api/events/generate`、`api/events/choose`、`api/endings/generate` 各命中 1 次。顺带把三个开关的体积贡献分离测了一遍：基线 285.11 kB，只开走查条 291.93（+6.82），只设 mock 288.60（+3.49），两个都开 293.41——**走查条才是大头**。
  这条探针当场抓到一个真事故：本机 `.env` 被手工编辑时丢了全部 `#` 注释符，于是原本作为说明留在文件里的 `VITE_API_MODE=mock` 变成一条生效赋值，前端**一个请求都不发**，表现就是负责人说的「没有调用接口」。根因不在代码也不在接口约定，但暴露了一个通用风险：**配置模板里不该留"注释掉的赋值行"**，因为它对任何一次手滑编辑都是敞开的。这条建议交给负责 `.env.example` 的人（见待协作 21）。
- 结果（档案归一，2026-09-12）：一个临时脚本把 `normalizeProfile` 的九种输入各打一遍，全部符合预期 —— 两项都填才返回对象；只填专业 / 只填性别 / 选「不填」/ `gender` 塞英文 `male` / 传 `null` / 传字符串 → 六种一律 `null`（即不发档案）；带换行的专业压成单空格、60 字专业截到 20 字。脚本放在仓库外，未入库。
- 结果（请求体形状）：源码级断言两处生成请求都走 `bodyWithProfile`，而 `/api/events/choose` 的字面量 `{ requestId, snapshot, eventId, optionId }` 未被改动（结算不调模型，所以不带档案）；产物探针在 `dist` 里搜到 `campusmock:profile`、「你的专业」、「不填」，说明存储键与开始页表单都没被摇掉。构建：JS 286.96 KB（gzip 91.73）、CSS 13.45 KB（gzip 3.94）。
- 结果（确认不会撞服务端）：`server/src/index.ts:21` 只有 `express.json({ limit: '1mb' })`，全仓库 grep 不到对请求体键名的白名单校验；`validateSnapshot` 与 D 的 `isSnapshot` 都是逐字段判已知字段。所以 B 接线之前，这份多带一个 `profile` 的请求照样能正常走完全程（静态核对，未打真请求）。
- **未做（如实记）**：新表单在 390 窄屏与 1440 桌面档的实际观感**没有截图核对**（本机权限拦着浏览器），输入框聚焦态、胶囊选中态、`aria-pressed` 的读屏表现只做到代码级；B 接线之后"日记真的按专业写了内容"这条更无从验起。
- 验证方式（错误屏拆分这轮）：本机起不了浏览器，改用 `react-dom/server` 的 `renderToStaticMarkup` 把 `ErrorPage` 按不同 `error.code` 静态渲染出 HTML，再对可见文案与按钮做断言（一次性脚本 `client/tmp-error-render.mts`，跑完即删、未入库）。这条路比读代码可靠：它走的是组件真实分支，只是没有样式。
- 结果（14 条断言全通）：`AI_CONFIG` / `STAGE_MISMATCH` 这类**未列举的服务端 code** 默认落到新的生成类屏（有「先回到上一页」、hint 是「这一步没有写入…」、无「清除坏档」）；`SNAPSHOT_VERSION_UNSUPPORTED` / `STORAGE_UNAVAILABLE` 仍落在旧的存档屏（导出+清除坏档、hint 未变、不出现「先回到上一页」）；`SAVE_CHANGED_IN_ANOTHER_TAB` 与可重试屏**逐字未变**。断言里排掉了一条假阳性：一开始拿「本机存储」那句话做判据，结果两个 FAIL —— 它在折叠的处理说明里，`showHelp` 初始为 `false` 时根本不渲染，改成断言始终渲染的 `hint`。
- 结果（新的不可重试屏能离线演）：`mockApi` 的 `FaultKind` 加 `'fatalServer'`，走查面板第四颗按钮「下次请求·服务端不可重试」返回 `AI_CONFIG` + `retryable: false` + **成员 C 原文照抄的 message**，与真链路同一屏；原来那颗 `fatal` 保持注入存档类 code，两屏现在分开可看。顺带把两颗按钮的标签改成「存档不可重试 / 服务端不可重试」，否则同名按钮指向不同屏。
- **未做（如实记）**：① 折叠的「处理说明」两套新文案只在源码分支里对，SSR 翻不动 `useState`，没有实际渲染出来核对；② `dismissError()` 在 `pendingEvent` / `pendingEnding` 下回开始页这条是**读代码确定的**（`screen` 的三元式 `!entered || !snapshot → 'start'`，`StartPage` 在有存档时给「继续上次的日记（第 N 天）」，点它走 `resume → stepFrom` 会重新发起这一步），但没在浏览器里点过；③ 两屏的实际观感与 390 档折行仍未截图。
- 验证方式（结算页等待反馈这轮）：仍然走 `renderToStaticMarkup` 那条路（一次性脚本 `client/tmp-game-render.mts`，跑完即删、未入库）。**一个坑记下来**：`tsx` 必须在 `client/` 目录下跑，从仓库根跑拿不到 `client/tsconfig.json` 里的 `"jsx":"react-jsx"`，`Chrome.tsx` 顶层那个 `<svg>` 常量会直接 `ReferenceError: React is not defined`。
- 结果（4 组 22 条断言全通）：`waiting=true` 时渲染出 `class="next"`、`role="status"`、「正在写第 6 天」、`class="turning"`、`class="bar"`，同时「继续写下一篇」、那行 hint、`class="btn"` **三者全部消失**——证明按钮是被换掉的，不是叠在下面；`waiting=false` 时逐字回到改动前的形态（「继续写下一篇」＋「第 6 天会在你点继续之后才开始」，不含 `next` / `turning`）。第 14 天那一组另外验了「正在写结尾」与「去写结尾」两种标签各自只出现在自己的分支里。
- 结果（构建）：`npm run typecheck --workspace client` 退出 0；`npm run build --workspace client` 140ms 通过，体积增量记在「产物实测」那条。
- **未做（如实记）**：① `.turning` 那层 `div` 包在 `ResultCard` 外面会不会改变卡片间距——块级外边距穿透无 border/padding 的 wrapper 理论上不变，但没量过；② 流光条在 390 窄屏的观感、`--energy` 压在 `--energy-soft` 轨道上的对比度没有截图；③ 「点下去真的看得见吗」这条最终要人在 dev 里点一次才算数，SSR 只能证明两种形态各自渲染什么，证不了真实请求期间 `busy` 为 true 的那一段有多长、会不会一闪而过。
- 验证方式（第 14 天结局死锁这轮，2026-09-12 下午）：仍然只有 `renderToStaticMarkup` 这条路（本机起不了浏览器），但这次是**两个**一次性脚本，跑完即删、未入库——`client/tmp-day14-render.mts` 验"每一格渲染什么"，`client/tmp-day14-chain.mts` 验"沿着 UI 自己的路由真走 14 天走得到 `ended` 吗"。后者是关键：只测渲染证明不了那颗按钮点下去会发出对的请求。
- 结果（渲染矩阵 26 条断言全通）：`phase ∈ {showResult, pendingEnding} × waiting ∈ {true, false} × busy ∈ {true, false}` 逐格断言。核心两格：`pendingEnding + waiting=false` 出第 14 篇 `ResultCard`（含 `chosenText`）+ 「去写结尾」+ hint，**页面上不存在 `class="wait"` / `class="spin"`**（当时那句"也不存在任何 `disabled` 按钮"只在 `busy=false` 时成立，`busy=true` 时那颗「去写结尾」合法地是禁用的，纠正见下面傍晚那条复验）；`pendingEnding + waiting=true` 才出 `EndingWaitingCard`。`showResult` 那四格与上一轮的流光条断言逐字未变（改动没有回退第六件事的东西）。
- 结果（全链路 61 条断言 / 29 次请求）：用 `mockApi` 从空档走完 14 天，每一步发哪个请求**由界面自己的判断决定**（结算走 `choose`，落在 `pendingEnding` 就走「去写结尾」那条 `continueDay`），末态 `phase === 'ended'`、`history` 14 篇、`revision` 与 mock 侧一致、结局字段齐。中途专设一格：把第 14 篇结算后的快照重新喂回状态机并让 `busy` 归 false（等价于玩家在这一刻刷新），断言渲染回的是「去写结尾」而不是骨架屏，点下去仍能推进到 `ended` —— 原记录里"刷新也救不回来"那半段一并验了。
- 结果（本轮构建）：`npm run typecheck --workspace client` 退出 0；`npm run build --workspace client` 153ms 通过。JS 288.46 KB（gzip 92.20）、CSS 13.93 KB（gzip 4.04），与上一轮几乎相同（+0.01 KB），改动是一个派生布尔加注释。
- **未做（如实记）**：① 以上全是静态渲染 + 纯函数链路，**没有在浏览器里真点一次第 14 天**，`busy` 从 true 落回 false 那一瞬的实际观感没人看过；② 链路脚本喂的是 mock 快照，真服务端那份 `pendingEnding` 快照的字段形状是读 `controller.ts` 的 `sendSuccess` 推出来的、**没抓包比对过**；③ 走查面板 `④ 待生成结局` 这屏换了内容，没重新截图；④ 390 / 1440 两档观感仍然没有图（同前几轮的限制）。
- 结果（2026-09-12 傍晚，主干上第 14 天仍未修的判定）：不是靠推断，是直接读 `origin/main` 两个文件——`GamePage.tsx` 搜不到 `settledScreen`、结算屏的门仍是 `phase === 'showResult'` 单条件；`controller.ts:206-207` 第 14 次 `choose` 仍返回 `pendingEnding`。两端各指一半，合起来就是"选完点不到任何按钮"。**即 #25 里不含 `f89a90b`，那次 push 失败直接把这条留成了线上缺陷**。
- 验证方式（并分支后的复验）：`git merge origin/main` 进 `feat/client-prose-serif`（`d2b542f`，无冲突），然后重跑渲染断言 + 类型检查 + 构建三件套。脚本仍是 `renderToStaticMarkup` 那条路（一次性 `client/tmp-day14-check.mts`，跑完即删、未入库），矩阵换成 `phase ∈ {showResult, pendingEnding} × waiting × busy` 共 18 条。
- 结果（18 条断言全通）：并完 `main` 之后第 14 天的两种形态各自渲染正确（`pendingEnding + waiting=false` 出第 14 篇结算卡 + 可点的「去写结尾」；`waiting=true` 才出结局等待卡）。**断言里纠正了我自己写反的一条**：`pendingEnding / waiting=false / busy=true` 那格我原本要求"不得出现 disabled 按钮"，实际应当出现——`App.tsx:63` 传的 `busy` 是 `interactionBlocked`，它在写档失败时也为 `true`，所以这一格合法地渲染一颗禁用的「去写结尾」，是按钮而不骨架屏。判"这一格该有什么"要回到 prop 的语义去推，别照直觉写。
- 结果（#26 对前端零冲击，用 hash 证明而不是读 diff）：`npm run typecheck --workspace client` 退出 0，`npm run build --workspace client` 672ms 通过，产物 `index-DMW56tej.js` 288.46 kB / `index-DJVxUdQn.css` 13.93 kB —— **文件名 hash 与并分支之前逐字相同**。Vite 的文件名带内容 hash，hash 没变即"客户端源码一个字节都没变"，比逐文件看 diff 更省事也更不容易漏。
- 结果（流式效果未实现，四处独立证据）：① `server/src/game/`、`server/src/routes/`、`server/src/index.ts` 与整个 `client/src/` 里搜 `generateEventStream` **零命中**——C 的 #23 只交付了 `server/src/ai/` 内部的流式底座，没有任何调用点；② 服务端搜 `res.write` / `flushHeaders` / `application/x-ndjson` 零命中，`deepseek.ts` 里只有 `:83` 与 `:175` 两处 `Content-Type: application/json`，即上游到服务端是流、**服务端到浏览器这一程仍然是整块 JSON**；③ 前端搜 `TextDecoder` / `getReader` / `ReadableStream` / `EventSource` 零命中；④ #26 未触及任何 `client/` 文件。**所以"界面上看得到字一点点长出来"这件事现在不成立**，三段（服务端调用点、传输、前端消费）都没接，我那张 NDJSON 帧约定（待协作 26）仍然只是约定。
- 验证方式（流式前端段这轮，2026-09-12 晚）：两个一次性脚本，跑完即删、未入库——`client/scratch/frames.tsx` 验帧的解析与拼装，`client/scratch/render.tsx` 验等待卡两种形态各画什么。**"没流式时零变化"这次是用逐字比对证的，不是靠读代码**：`git show HEAD:client/src/components/Cards.tsx` 复制成 `src/components/_oldCards.tsx`（放同目录才解析得动它那些 `../state/…` 相对导入），新旧两张卡各 `renderToStaticMarkup` 一次，摘掉 `<div class="note">` 之后要求字符串完全相等。补充一条 `tsx` 的坑：脚本放在 `client/scratch/` 就落在 `tsconfig` 的 `include` 之外，esbuild 会退回经典 JSX 变换，不 `import * as React from 'react'` 就直接 `ReferenceError: React is not defined`（和上面那条"必须在 `client/` 下跑"是两个独立原因）。
- 结果（帧层 10 条断言全通）：① 中文 NDJSON 按 1/2/3/7 **字节**切断，帧数与还原出的标题正文都不变形（走的就是 `TextDecoder(…, {stream:true})` + 行解析器这两步）；② CRLF 分行、末行不带换行的终帧照样收到；③ 坏 JSON 行丢弃、未知 `k` 忽略，正文仍然完整（含 `\n\n` 被转义在帧里这种情形）；④ `reset` 把草稿清空；⑤ `title` 整段覆盖 / `desc` 往后接；⑥ `v` 不是字符串的帧不污染草稿；⑦ 断流没有终帧时解析器不伪造结果；⑧ 假服务逐帧输出时**所有中间帧都早于响应**、拼起来等于终帧里那个事件、`revision` 只 +1；⑨ 不传回调时假服务一帧也不发；⑩ 注入故障那一次不发中间帧（错误屏不会留半截日记）。假服务第一天实测 6 帧 / 997ms。
- 结果（渲染 6 条断言全通）：不传 `draft` / 传空草稿 / 主干上那张老卡，**玩家可见部分三者逐字相同**（骨架屏、「台灯已经打开了，日记还在路上……」、「等待不会消耗这一天」都在）；有草稿时骨架屏换成标题 + 按空行切开的正文、`class="body typing"`、等待文案改成「笔尖还在往下走……」而那句安抚话保留；只有标题到达时不留空 `<p>`，只有正文到达时不渲染空 `<h2>`。
- 结果（本轮构建）：`tsc -b` 退出 0；`vite build` 129ms 通过，产物 `index-AUQOt78j.js` 290.10 kB（gzip 92.93）/ `index-n9nGZjek.css` 14.24 kB（gzip 4.12）——比傍晚那一版 JS +1.64 kB、CSS +0.31 kB，就是读帧循环、帧解析器、草稿分支和一支笔尖。
- **未做（如实记）**：① **`gameApi.ts` 里那个 `readFrames` 本体没有被真正执行过**——它所在的模块读 `import.meta.env`，`tsx` 直接 import 会炸，而我不为了测试去改产品代码的取值方式。脚本里是把它的两步（`TextDecoder` 逐块 + 行解析器）原样拼了一遍，覆盖的是最可能出错的地方（切片边界、半行、CRLF、坏行），没覆盖的是 `response.body` 读取循环与"没有终帧就抛"那一句；② **真服务端至今不返回 NDJSON**，所以整条流式路径在真实 HTTP 上从没跑过一次，今晚成立的只有"前端吃帧"这一半；③ `settled` + `requestGeneration` 那两道闸是读代码确定的（帧只可能在 `await` 期间到达，一旦派发新请求 `token` 必然不等），没有构造过一次真实的竞态；④ 被顶掉的那条流仍在后台读完、没有 `AbortController`，这是改动前就有的行为（原代码同样只是丢弃过期响应），我没有顺手扩大改动范围；⑤ 笔尖在 390 窄屏的实际观感、正文一边长一边把页面顶下去会不会跳，都没在浏览器里看过。
- 结果（2026-09-12 深夜，**上一条里"今晚就能在 dev 里看到这个效果"当时是不成立的**）：负责人对着截图问"点完继续写下一篇为什么还是等待画面"，回头读 `GamePage.tsx` 才发现我把草稿挂错了屏——`EventWaitingCard` 只在 `phase === 'pendingEvent'` 那一支渲染，而 `dispatch()` 全程不改 `phase`（真快照要等响应回来才由 `persistAndApply` 换掉），所以从结算页点「继续写下一篇」发起下一次生成时，屏幕上一直是 `showResult + waiting=true` 那一支（流光条 + 「正在写第 13 天」），逐帧到达的字**根本没有渲染它的地方**。`pendingEvent` 那一支只在"生成中途刷新"时才走到，是条窄路。**这条缺陷与 B 无关，即使他今晚就把 NDJSON 接上，玩家也看不见字**——所以先修我自己这半边。
- 结果（修法）：`Cards.tsx` 把等待卡里"标题 + 按空行切开的正文 + 笔尖"那一段抽成导出的 `DraftText`（等待卡改成调它，渲染结果逐字不变），`GamePage.tsx` 的 `waiting` 分支在流光条下面加一格 `<div className="card streaming">`，**门是 `draft && hasDraft(draft)`**：一帧都没到（服务端仍返回整包 JSON）时这个 `div` 不存在，画面与改动前逐字相同。第 12 篇结算卡保留在它上面并被 `.turning` 压暗，「正在写第 13 天」那行也保留——玩家始终知道在写的是哪一天。CSS 只多一行 `.streaming{margin-top:14px}`，顺带改掉上一条注释里"按钮塔成"的错字。
- 验证方式：一次性 `client/scratch/wiring.tsx`（`renderToStaticMarkup`，跑完即删、未入库）。基线是把 `git show HEAD:` 的 `Cards.tsx` 与 `GamePage.tsx` 各抽成 `_oldCards.tsx` / `_oldGamePage.tsx` 放在**同目录**（相对 import 才能原样解析），新旧并排渲染比字符串。11 条断言全通：A 组两条证明 `DraftText` 抽出对等待卡零冲击（有帧、空草稿各一次逐字相同）；B 组证明结算屏在空草稿、不等待两种形态下与改动前**逐字相同**，并反向确认改动前那一版即使喂草稿也不会出现 `streaming`（否则这条断言是空的）；C 组证明有帧时结算屏真的出现 `class="card streaming"` + 标题 + 两段正文 + `typing`，同时 `class="next"`、「正在写第 13 天」、`class="turning"` 三者仍在、第 12 篇结算卡排在流式卡之前，且**不等待那一屏不会因为残留草稿而长出字**。
- 结果（本轮构建）：`npm run typecheck --workspace client` 退出 0；`npm run build --workspace client` 123ms 通过，`index-XtBnbkpT.js` 290.29 kB（gzip 92.96），比上一版 +0.19 kB，改动是一个抽出的组件加一格条件渲染。
- **未做（如实记）**：① 依旧**没有真 NDJSON 到达过**，所以"字会在结算屏下面一段段长出来"目前只在静态渲染层面成立——喂的是我手写的草稿对象，不是走完整条 `readFrames`；② 流式中途失败（帧到一半连接断）时结算屏会留一张写了一半的卡，界面靠 `dispatch` 抛错切到错误屏，这条没演过；③ 390 窄屏上"上一天卡片 + 流光条 + 正在长的这一天的字"总高度会顶出可视区，一边长一边滚到底好不好看没人看过；④ 第 14 天那一路（`generateEnding`）C 没有 `generateEndingStream`，所以那一屏仍然只有流光条。
- **归属重新说清一次（免得催错人）**：这条修完，前端三段（读流、草稿态、渲染）**全部就位**；玩家界面上还看不到流式，剩下的唯一原因是服务端 `/api/events/generate` 仍返回整块 JSON——要的是 B 的 `controller.ts` 改调 `generateEventStream`，加 `res.writeHead(200, {'Content-Type': 'application/x-ndjson'})` 与 `res.flushHeaders()`，并在**同一张 PR** 里改掉那条按整包 JSON 读响应的冒烟断言。今晚想先看到效果：把 `.env` 的 `VITE_API_MODE` 设成 `mock` 重启 dev 即可（假服务已改成在它原本就要等的那 420~1150ms 里分帧吐字），代价是文案换成 `client/src/api/script.ts` 那批剧本字，**别拿它当模型输出给人看**。
- 结果（2026-09-12 晚 21:30 再拉一次远端，为"流式还要不要改"这个问题取证据）：`origin/main` 只多了我自己的 #28（`c55b21f`），**第 14 天死锁从此闭环**；C 的 `origin/feat/AI` 动了 5 个文件（`deepseek.ts` +2 是重试日志、两个提示词、两份文档），**没有一行碰 HTTP 传输**。判定服务端段仍未接用的是两条 grep 各打两个 ref：`generateEventStream` 在 `origin/main` 与 `origin/feat/AI` 上都只出现在 `server/src/ai/` 内部与 `ai/README.md`，`res.write` / `flushHeaders` / `x-ndjson` **两边都零命中**。**我这侧真正要改的只有一处，而且是我的文档不是代码**：`EventStreamHandlers`（`generateEvent.ts:35-42`）一共 6 个回调，我在待协作 26 里只列了 4 个，漏掉 `onResultTextDelta` 与 `onResultText`——B 照着 4 个接会当场发现对不上、然后来问我，所以把这两个的帧名与取舍（**可以不发；发了前端也只收下不画，因为结果叙述要等选完才出现在结算页，那时终帧早已到齐**）补进约定。代码一行未动：`applyDraftFrame` 对未知 `k` 原样返回草稿，这两帧今天发过来也不会弄坏界面，这一点是读代码确认的、不是推测。
- 仍存在的问题：深色模式没做（不在本轮范围）；动效只确认在跑，低端局域网设备上的帧率没测；`⑥⑦` 的观感待人眼确认（上一条）；模型接线后如果 C 放宽 `TEXT_LIMITS`（注释里写着"草案值，实测后调整"），顶格夹具要跟着改一次上限，两处数字都在 `server/src/ai/prompts/values.ts`，别在夹具里写死第二次。**真实模型输出已经肉眼看到了（2026-09-12 中午，比上一版这条记录更新）**：成员 A 在 `localhost:3000` 上看到第 1 天事件标题「初入校园的黄昏」，用三处排除法确认它不是假数据——不在前端 mock 剧本里（`client/src/api/script.ts:38` 第 1 天写的是「报到那天，我没敢问宿管阿姨要钥匙」）、不在服务端兜底文案里（`server/src/ai/fallback.ts` 第 1 天按 `day % 4` 命中的是「食堂新开的窗口」）、也不在构建产物里（`grep -c 初入校园的黄昏 client/dist/assets/index-*.js` 为 0）。所以 **当时那次**（#19 之后、#24 之前，`loadAiConfig()` 还在请求阶段）密钥与读配置的链路是通的。**#24 把配置读取挪回模块加载期后这条一度重新不成立，傍晚 #26 已修好**（新增 `server/src/env.ts` 按 `__dirname` 定位根目录 `.env`，见待协作 27 的闭环记录）⇒ 现在 `npm run dev:server` 直接可用，先前"必须从仓库根 cwd 起服务"的绕法作废。**剩下的未验证项收窄成一条**：衬线排版在真实字量下的观感还没逐屏核对，而那一局是续档开的、没填档案，看到的还是中性表述分支。**原来那条"档案缺 B 两行"已经过期**：B 在 #24 接上了（待协作 23 前三小点），所以「界面能填」现在确实等于「服务端会喂进提示词」；未验证项换成下一条——**填了档案与不填，生成的日记到底有没有区别**，需要开两局对照才能看出来，**而这一条现在不被任何缺陷挡着**（#26 之后配置读取正常），缺的只是有人开两局各填一次。**最后一条会挡演示的未修项已经闭环**：第 14 天选完永久卡在结局骨架屏（待协作 25）——修复随 **PR #28 在 19:20 合进 `main`（`c55b21f`）**，主干不再坏。**现在真正还开着的只有两条**：~~流式的服务端那两段（调用点 + 传输，归 B，见待协作 26）~~（**深夜已闭环，见下面第一条**）与局域网真机验收（归 D）。
- 结果（2026-09-12 深夜 23:20，拉主干后逐行核过，不是听说的）：**流式端到端在主干闭环，待协作 26 交出去的东西 B 全接了**。`origin/main` = `ee6b839`；我的 #30 由 `bb77803` 合并；B 的 #32（`d120dc8`）在 `server/src/game/controller.ts:138-144` 写 `writeHead(200,{'Content-Type':'application/x-ndjson','Cache-Control':'no-cache','X-Accel-Buffering':'no'})` + `res.flushHeaders()`，`:148` 调 `generateEventStream`，`:93` 的 `writeFrame` 就是 `res.write(JSON.stringify(frame)+'\n')`。**只发 `title` / `desc` / `reset` 三帧**（注释明写「`onOptionText` / `onResultTextDelta` 前端暂不画，不写帧」），终帧两种形状（成功 `{k:'end',requestId,baseRevision,snapshot}`；效果越界与异常 `{k:'end',requestId,error}`）与我的闸门逐字对得上——`isApiResult` 只查字段，多一个 `k` 不影响。**我要求的"CI 必须同一张 PR 改"他也做到了**：`server/tests/ai-integration.cjs:152` 按 `content-type` 分流读帧，主干 check-run `conclusion: success`。上面「未做（如实记）」里第 ① 条那句"从未收到过真 NDJSON"就此作废。
- **同一张 PR 里他把我的渲染方案换掉了，动的就是我的文件**：`GamePage.tsx:26` 现在是 `phase = snapshot.phase === 'showResult' && waiting ? 'pendingEvent' : snapshot.phase`——点「继续写下一篇」整页切到下一天的等待卡，字长在那张卡上；我 `cdbc65e` 挂在流光条下面的 `hasDraft(draft)` 块被删。**可证的后果**：`settledScreen ⟹ !waiting` 恒成立 ⇒ `GamePage.tsx:58` 的 `.turning`（上一页压暗）与 `:61-67` 的 `.next`（流光条）两支永远不会渲染，`global.css` 的 `.streaming` 已 0 引用。这不是 bug，是观感决策，记在这里免得下次有人来问那条流光条去哪了。**教训**：`luoyu-0` 会在主干上直接重写 A 的实现，所以"我修好了 X"这类句子过期极快，判定必须以 `git diff <我的提交> origin/main -- client` 为准。
- 结果（2026-09-12 深夜，等待卡的转圈换成笔尖插画）：`Cards.tsx` 新增 `PenRest`，「待生成事件」与「待生成结局」两屏共用；`global.css` 里 `.wait .spin` 与 `@keyframes sp` 删除。换掉的理由是旋转和"写日记"毫无关系，而这一屏恰好是玩家盯得最久的一屏。**做法是纯 SVG + CSS，没有引 Lottie**——多一个依赖和一个运行时，在这五天里不值得。画面：米黄纸 `#fffdf7` + 四条淡横线 `rgba(120,110,92,.2)`（这两个色值沿用背景那些漂浮纸页，同一屏不能出现两种纸）、金属渐变笔尖（带中缝与通气孔）、笔尖处暖橙光晕按 `--energy` 呼吸、下方一道 `--brand` 蓝墨迹沿横线缓慢延伸，墨迹末端一点湿墨向外晕开。**动效全部只有位移与透明度，一帧旋转都没有**；四个周期 2.8 / 2.6 / 6.5 / 9 秒互不成整数倍，盯久了不会看出循环；墨迹写完用淡出收尾而不是在满格处硬跳回空白；`prefers-reduced-motion` 下静止成"已写满"那一帧。
- 上面这条的验证方式与未做（如实记）：`react-dom/server` 把两屏各渲染一次做 17 条字符串断言全通过（有草稿 / 无草稿两支、`spin` 节点与 CSS 规则都已消失、三个渐变 id 与 8 档 stop 色值都在、`pathLength="100"`、`aria-hidden`、四条横线、层级顺序为光晕 → 湿墨 → 笔杆），`npm run build` rc=0。**但这些都是字符串层面的，没有一条是眼睛看到的**：本机起服务与开浏览器都被拦，连 `file://` 预览页也没能替我截图。所以笔尖相对正文是否偏大、光晕在纸上够不够柔、墨迹延伸的速度在真机上是否够静，全待人眼确认；要调只动 `global.css` 的 `.wait .pen` 那一节，颜色与节奏都在里面，形状在 `PenRest`。
- 结果（2026-09-13 凌晨，"还是有点死板"那一轮微动态）：五条全部落地，**没有一条是靠感觉写的，动效之间的对应关系是算出来的**。① 环境呼吸：光晕改成 `opacity .6↔1` / `3.4s`，纸边 `translateY(-1px)` / `7.5s`，且 1px 那条挂在 `.pn-sheet` 上、**墨迹在这个组里面**——挂在纸上而笔不挂，字才会跟着纸一起浮。② 背景微动：两片 `pn-blade` 半透明叶子，15s / 21s 两条下落轨迹带自转，`cm-pen-fall` 每一档都写 opacity，落到底之前先淡掉（不淡的话循环点那一下"叶子凭空出现在纸上方"比飘更显眼）。③ 拟人化书写：`cm-pen-hand` 12 秒一圈、18 档 `translateX`，三段快写之间夹两次几乎不动的顿住（22%→30% 只走 2 单位），每段都吃 `ease-in-out` 所以进出每一档都会减速——**顿挫感就是这么来的，不是加抖动**。换行是三条动画错拍叠出来的弧：`cm-pen-lift` 抬 7 单位（40%→43%）、`cm-pen-hand` 在空中飘回左边（40%→49%）、`cm-pen-line` 落到下一根横线（42%→46%，位移 18 = 两根横线的间距），落笔在 52%。④ 流式墨迹晕染：`.ink-in` = `scale(.8)→1` + `blur(2px)→0` + opacity，`transform-origin:left center`（不靠左的话整行会往中间缩，看着像抖）。⑤ 思考态：`.pen.is-thinking` 把走纸/换行/抬笔/两条墨迹全部 `animation-play-state:paused`，笔尖只剩 `cm-pen-tap` 原地点触、暖光换 `cm-pen-flicker` 与它同拍。**"冻住"而不是"撤掉动画"是必须的**：撤掉会让笔瞬移回原位、墨迹瞬间清空，那一下比匀速转圈还难看。
- 同一轮的契约与接线（改动了 6 个文件，逐条可查）：`api/frames.ts` 的 `StreamDraft` 多两个字段 `fresh`（最新到的那一段正文）与 `chunk`（到齐过几段），只为渲染层服务、**不进快照也不进 localStorage**；`DraftText` 只把 `fresh` 包成 `<span className="ink-in" key={chunk}>`，靠换 key 重放动画，`paragraphs()` 会 trim 所以尾巴落在换行上时拿不到 `endsWith` → 退回普通渲染，**宁可不动画也不能掉字**（这条有断言）。思考态由 `state/useGame.ts` 算：`FRAME_IDLE_MS = 1200`，`dispatch` 起计时、每来一帧重置、请求结束时撤——**撤之前要判 `token === requestGeneration.current`**，否则慢掉的旧请求会把新请求刚挂上的计时器撤掉。阈值 1.2 秒是照着假服务的节奏定的（`mockApi` 把 420~1150ms 均分给六七帧，两帧之间最多两百来毫秒），真模型首帧延迟没测过。`EndingWaitingCard` 恒为思考态，因为 C 那一路确实没有 `generateEndingStream`，那一屏永远收不到帧。链路：`useGame → App.tsx → GamePage → EventWaitingCard → PenRest`，只多一个布尔 prop。（**这条链路和 `DraftText` 的落点都已作废**：负责人 #35 把生成态折进 `EventCard`，见最后一条。）
- 上面这条的验证方式与未做（如实记）：`react-dom/server` + 直接读 `global.css` 做 **41 条断言全通过**，`npm run build` rc=0。其中三条是这轮真正有价值的：**把墨迹每一档 `stroke-dashoffset` 按笔尖位置反算了一遍**（线一 `100*(1-(x-30)/120)`、线二 `/80`），误差全部 ≤2，所以对不上"字写完了笔还在别处"这件事是被证伪的、不是被祈祷的；另两条是"四层组必须按 hand→line→lift→tip 嵌套"和"画里每个类名在 CSS 里都有规则"（这条当场逮到一个失去规则的 `pn-nib`，已把那个空壳组删掉）。**仍然没有一条是眼睛看到的**：本机起服务、开浏览器、`file://` 预览页三样都被拦。给人眼看的入口已经重写：预览页 link 的就是仓库里那份 `global.css`，形状从 `PenRest` 逐字抄过去，脚本按假服务同款节奏自己长字，另有「重播」与「冻住 / 放开」两个按钮。**改了 `PenRest` 记得去预览页同步一次，它不会自己跟着变。** 待人眼确认的收窄成四条：12 秒一圈对"一秒多就出字"的短请求意味着玩家多半只看得到第一行的前三分之一（要不要把整圈压到 6 秒）；两片叶子在 390 窄屏会不会和正文打架；`prefers-reduced-motion` 那一帧静止画面好不好看没看过；真模型首帧若慢于 1.2 秒，玩家会先看到"在想下一句"——不算说谎但也不算准。
- AI 生图那条我试了，结论是**这张不能替掉 loader，建议只当素材留着**：生成物是一张水彩书桌（米黄纸 + 金属笔尖 + 暖灯 + 淡蓝墨点 + 飘叶，配色和界面是同一路，肉眼判断它确实比我现在这画好看），但三个硬伤——右下角带「AI 生成」水印；笔是斜躺在纸上的**静态构图**，换成它就等于放弃这一轮要的抬笔/换行/点触；文件 1.06 MB，而它在界面上只有 158px 宽，细节全糊。**位图和"会动的笔"这两件事只能选一个**，所以画面留矢量、要 AI 插画就该给面积更大的地方（开始页或结局页横幅）。要用的话得先裁掉水印并压到几十 KB，那是一次独立的改动，今晚不动。
- 结果（2026-09-13 凌晨更晚，**上面三条做完的这段时间里主干又走了两个 PR，我的动效得重新接一次**）：`origin/main` = `a4b37c5`。#34（`d0b0d9f`，23:35）纯服务端，一行没碰 `client/`；动我这块的是 #35（`17d1c92`，23:52，「解决事件选项生成造成的页面重挂载导致的观感不佳问题」）——`EventCard` 入参从 `event: GameEvent` 换成 `event: GameEvent | null` 并新增 `day` / `draft`，`GamePage.tsx` 把 `pendingEvent` 与 `pendingChoice` 合成一支卡，`App.tsx` 的 `.screen` 改按 `pageKey` 而不是 `screen` 当 key，`useGame.ts:251` 顺手让 `choice` 沿用 `generating` 的背景。**他给的理由和我要的是同一件事**：终帧到达时整卡重建会刚长出来的字冲掉——那正好也是洇墨要活的唯一前提。所以我这边没有第二条路，只能把动效挂到他这张卡上。
- 他那一版里有三处我必须表态，记下来免得下次重新讨论：① `EventCard` 的等待块放的是 `<div className="spin" />`，也就是**转圈在主干上回来了**（`origin/main` 的 `global.css:245` `.wait .spin` 与 `:250` `@keyframes sp` 都还在，我这条分支删掉了它们），而同一个块下面那行字写着「笔尖还在往下走……」——**字在说笔尖、画是圆圈，两边对不上**；② 他**没有删** `EventWaitingCard` 与 `DraftText`，`GamePage.tsx:2` 的 import 里已经没有这两个，合完之后它们是 0 引用的死代码（**我中途一度判成"他把等待卡删了"，事实是他把调用方摘了、组件留着**）；③ 骨架屏 `.skeleton` 唯一的调用方就是那张死掉的等待卡。
- 我这轮的处置（`git merge origin/main`，冲突只有 `GamePage.tsx` 一处）：**接受他的单卡结构**，把 `PenRest` + `thinking` + `.ink-in` 挂回 `EventCard`，判据沿用他的 `!event`（等价于"还在生成"）；那个 `spin` 的 `div` 换成 `<PenRest thinking={thinking} />`；等待文案从他的两态补成三态（无草稿 / 在写 / 在想），思考态是他那条分支上没有的东西；`EventWaitingCard`、`DraftText` 与失去消费者的 `.skeleton` + `@keyframes sh` 一并删除；那两条"为什么"（chunk 尾巴落在换行上就不洇、等待不推进天数是硬验收项）从被删组件的注释挪进 `EventCard`，不随删除一起丢掉。**他的 `.option-reveal` 与 160ms 递增延迟一行未改**（那 8 行 CSS 加在文件末尾，与笔尖那一节无交集）。
- 验证方式与未做（如实记）：`tsc --noEmit` 退出 0；`vite build` 179ms 通过（`index-C9yKkkZH.js` 292.89 kB / gzip 93.83，`index-DyBsWDBs.css` 19.72 kB / gzip 5.21）；对构建产物 `grep` 确认 `spin`、`skeleton`、`@keyframes sp` 都已不在包里，而 `cm-pen-hand`、`ink-in`、`pn-tip` 都在。一次性 `client/scratch/pen-probe.tsx`（跑完即删、未入库）**29 条断言全通过**：A 组 13 条打在生成中的 `EventCard`（笔尖在 / 无选项 / 三态文案 / 标题与刚落那段各洇一次 / 文字一字不少且不重复计 / 尾巴落在换行上时退回普通渲染 / 只有最新一段在洇），B 组 5 条打在终帧（`wait`、`pen`、`ink-in` 全部消失，三个选项与提问行在，`<p>` 数等于 `paragraphs()` 数），C 组结局页恒为思考态，D 组 8 条是 CSS 与 JSX 双向对表，E 组仍是把墨迹每档 `stroke-dashoffset` 按笔尖位置反算（误差 ≤2）。**这轮逮出来的两条错都在验证脚本自己身上，不是产品缺陷，但性质值得记**：一是收集动画名时把 `animation:none` 里的 `none` 当成一个动画名去查 `@keyframes`；二是**按"到下一个换行 + 右括号"切 `@media` 块不可靠**——`@media (prefers-reduced-motion:reduce){.ink-in{animation:none}}` 这种单行写法的块尾不在换行处，于是"D6 关掉动效时笔尖全停"一度是在一个吞掉了后面几十行普通规则的超集里查类名，属于**假通过**；换成花括号配对扫描之后 D6 才真的成立，并顺带补了一条 D7 确认 `is-thinking` 那两条动画排在 reduce 块之前（靠源码顺序被压住）。**仍然没有一条是眼睛看到的**：起服务、开浏览器、`file://` 截图三样在本机都被拦；预览页在工作区根、不入库，已与 `PenRest` 逐类名对齐（两边各 21 个 `pn-*`，`sort -u` 后完全一致）。**本轮三个提交（`eb8b7c2`、`9854fb1` 和这次 merge）都还在本地，未 push、未开 PR。**
